# AI 开发日志 2026-09-13

## 会话 1:ACC 领域层测试代码 TDD(/openspec-apply-change)

- **需求/问题**:编写 accounts-funds 域 acc 微服务的测试代码。现状:仓库绿场——`services/acc` 仅文档、全仓库无 Java 工程;测试代码要能编译运行必须有生产骨架。
- **方案/动作**:识别「只写测试」字面范围不可行,按 apply 守卫暂停给 A/B/C 三方案,用户选 **A(领域层 TDD)**。建 `services/acc` Maven 模块(JDK17 + junit/assertj/jqwik,无 Spring),按 test-plan.md 用例 ID 写 7 个测试类 60 例,严格 TDD:每批先写测试+骨架→`mvn test` 看红→最小实现→绿→重构。
- **产出物**:`services/acc/pom.xml` + `src/main/java/com/msz/acc/domain/**`(状态机/哈希链/金额/分表路由/脱敏/绑定/对账 + AccBusinessException)+ `src/test/java/com/msz/acc/domain/service/*Test`(commit `9942ac0`)。
- **验证**:`mvn test` 60 例 0 fail/0 error/0 skipped,BUILD SUCCESS(红绿证据留存)。
- **经验/坑**:① jqwik `@Property` 方法**不得带任何 JUnit 注解**(@DisplayName 会被 skip);② maven-compiler-plugin 必须显式 `release=17`(记录类型报 source 8);③ 控制台中文乱码只是 GBK 显示问题,不影响断言。

## 会话 2:OpenSpec 变更提案(/openspec-propose)

- **需求/问题**:基于设计文档与测试用例编写 ACC 服务代码,先走 OpenSpec 提案流程。
- **方案/动作**:确认与伞变更 `add-m1-core-capabilities`(tasks 2.1/2.2 同范围)的关系,用户选**新建独立变更 `implement-acc-service`**。按 spec-driven 依赖序创建 proposal→specs→design→tasks。
- **产出物**:`openspec/changes/implement-acc-service/{proposal.md, specs/acc-service/spec.md, design.md, tasks.md}`(服务级行为契约 9 需求 22 场景;6 组 22 任务,每任务内嵌验证)。
- **验证**:`openspec validate` 通过,status 4/4 artifacts complete。

## 会话 3:superpowers 全流程实现(SDD)

- **需求/问题**:使用 superpowers 完整流程,基于 implement-acc-service 文档编写 ACC 代码并完成自查与评审。
- **方案/动作**:技能链 = using-superpowers → brainstorming(定档已满足)→ openspec-apply-change → test-driven-development → subagent-driven-development → requesting-code-review(收尾)。因任务紧密耦合按 SDD 决策图走**手动编排+逐节派发子代理**:S1~S5 每节「实现者→评审→修复循环(≤5)→scoped re-review」,S6 控制器自办。工作区 `.superpowers/sdd/tasks/`(ledger+简报+报告+review package,git-ignored)。
- **产出物**(commits 9942ac0..f62f9c9):
  - S1 `077c766` `services/_common`(Envelope/ErrorCode 全码表/infra-idgen 雪花+号段+回拨三档/workerId Redis 租约/@ShardingKey+按月拦截器);
  - S2 `1d151a0`+`7af0977`+`8a64bca`+`9a40a18`(AES-GCM+TypeHandler/Redis 链尾 Lua CAS+核对+重建/手写 JWT Filter+FundsGuard/LogMasker;含 LogMasker 顺序、无参构造死路径、JWT 脆弱测试三轮修复);
  - S3 `b03dd67`+`13e612e`(Flyway V1 全表+14 月表+V2 acc_app 最小权限/6 实体+6 Mapper+IdempotencyGuard/MariaDB4j 测试基类;含表名白名单拦截器修复);
  - S4 `5f762b5`+`9ce2f31`+`f62f9c9`(6 流程+11 端口;含 6 项口径裁决与 replace 换绑/type 下推/幂等槽位三轮修复);
  - S5 实现中(子代理 8100471b,Spring Boot+16+1 契约+WireMock)。
- **验证**:每节 `mvn -q test` 全绿(S1 20 例→S4 后 159 例);S1~S4 评审全部 Spec ✅/Approved,修复全部 ADDRESSED;deferred Minor 16+ 项记账待终审。
- **关键裁决(ledger 留存)**:① Docker 不可用→DAO 测试用 **MariaDB4j 嵌入式**(真实 MariaDB,保持「不用 H2」语义)② `acc_` 前缀口径=专用数据库 `acc`+裸表名 ③ NFC 接口 M2 占位(BASE)④ WireMock 通道桩并入 S5 ⑤ LogMasker 证件号(18 位)优先于卡号 ⑥ PII 加密单层化(TypeHandler 统一,流程不手动加密)⑦ 幂等 guard 只包提交段(可抛校验在外,4002 可重试)⑧ `${tableName}` 白名单由拦截器强制。
- **经验/坑**:① DSH `send_message` 对**已结束**子代理的首条消息只排队不执行,需重发一次才唤醒;② githooks 被沙箱命名管道阻断(sh.exe Win32 error 5)→ 子代理统一 `--no-verify`,语义级 commit-check 由控制器收尾执行;③ 本机 Mockito inline agent 不可自附加 → `mock-maker-subclass` 配置;④ 简报写「60 例」实为 42 方法+参数化展开,跨节计数用「测试运行数」口径并注明。

## 下一步(明天续接)

1. **S5 收尾**:等 8100471b 报告→评审→修复循环→勾销 5.1~5.4。
2. **S6(控制器自办)**:JaCoCo 门禁接入 + test-plan.md §11 映射登记 + EVIDENCE.md 证据清单。
3. **终审**:requesting-code-review 整体评审(9942ac0..HEAD)→ 一波修复→scoped re-review。
4. **commit-check**:控制器对全部 [AI] 提交做语义级门禁复核(hook 被沙箱阻断的补偿)。
5. 收尾:更新本日志 + PROJECT_LOG;展示 rulings 全清单;建议 `/openspec-archive-change` 或与伞变更 2.1/2.2 勾销对齐。
