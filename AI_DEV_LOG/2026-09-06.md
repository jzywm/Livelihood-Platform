# 2026-09-06 · AI 开发日志

## 会话 1:仓库整理为 monorepo 架构

**本次目标**:把纯文档仓库重构为 monorepo(PC React+Electron / 移动端 uni-app / 后端 Spring Boot 微服务)。

**过程记录**

1. 需求/问题:用户要求「将当前仓库整理为 monorepo:前端 PC=React+Electron、移动端=uniapp、后端=Java Spring Boot 微服务」,并明确「执行前遇到不明确处必须先确认」。
2. 方案/动作:摸底现状(纯文档仓库、无代码、非 git)→ 给 4 方案(轻量骨架 / 标准可运行脚手架 / 重量工程化 / 边界优先),用户选「方案 2 标准」。
3. 方案/动作:逐项确认 → 英文命名 apps/services/docs;文档统一收进 docs/;后端先单聚合服务;design 两处合并;日志目录留根;后端命名 server + com.minsheng.platform。
4. 方案/动作:PowerShell 迁移目录 + 手工写脚手架文件(**不执行 pnpm install / mvn**)。
5. 验证:最终 tree 核对结构正确。

**产出物**
- 迁移 `需求调研/`、`市场分析/`、`agent-sdlc-standard/`、`ai-presets/`、`llm-wiki/` → `docs/`
- 合并根 `design/`(用例图 + session-*)→ `docs/design/`
- `apps/pc/`(React18+Vite+Electron)、`apps/mobile/`(uni-app Vue3)、`services/`(Spring Boot 3.5.3 单聚合 server)
- 根 `package.json`、`pnpm-workspace.yaml`、`README.md`、`.gitignore`、`.editorconfig`、`services/README.md`(7 域预留拆分)

**关键决策与理由**
- 英文命名:git/工具链友好
- 后端先单聚合 + 按域分包:先单体后拆
- `AI_DEV_LOG`/`PROJECT_LOG`/`AGENT_MEMORY.md` 留根:技能硬编码根路径

**经验与教训**
- 沙箱直连 npm/GitHub raw 被 TLS 拦截(见会话 2),uniapp 版本用已知稳定 build 3.0.0-4020920240930001
- uniapp 约定 pages.json/manifest.json/pages 放 src/ 下(初建目录位置放错,已修正)

**下一步**:用户本机 push(见会话 2);联网后照 `apps/mobile/README.md` 升级 uniapp 版本。

## 会话 2:绑定 GitHub + Gitee 双远程

**本次目标**:git init + 双远程 + 首次提交。

**过程记录**

1. 需求/问题:用户给 GitHub `jzywm/Livelihood-Platform` 与 Gitee `jia-ruihang/livelihood-platform`,要求绑定。
2. 方案/动作:探测环境(git 2.52 ✅、全局身份已配置 ✅、外发 HTTPS 被 schannel 拦 ❌)→ 确认 origin=GitHub、两仓库为空、身份用全局。
3. 方案/动作:`.gitignore` 补排除工具目录;`git init -b main` + 双 remote + 首次提交。
4. 验证:git status 干净、remote -v 正确、误纳工具目录检查为空、205 文件提交成功。

**产出物**
- 首次提交 `7c3c429 chore: init monorepo structure`(205 文件,17468 行)
- `.gitignore` 补排除 `.downloads/`、`.dsh/`、`.npm-cache*/`

**关键决策与理由**
- origin=GitHub、gitee=Gitee;分支 main;身份 jia ruihang <584431019@qq.com>

**经验与教训**
- 沙箱外发 HTTPS 被 schannel 拦(SEC_E_NO_CREDENTIALS)→ push 只能用户本机执行
- git quotePath 对中文文件名转义是正常显示,非真实文件名

**下一步**:用户本机执行 `git push -u origin main` + `git push -u gitee main`。
