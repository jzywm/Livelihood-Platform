# 2026-09-06 · AI 开发日志

## 会话 1:仓库整理为 monorepo 架构

**本次目标**:把纯文档仓库重构为 monorepo(PC React+Electron / 移动端 uni-app / 后端 Spring Boot 微服务)。

**过程记录**

1. 需求/问题:用户要求「将当前仓库整理为 monorepo:前端 PC=React+Electron、移动端=uniapp、后端=Java Spring Boot 微服务」,并明确「执行前遇到不明确处必须先确认」。
2. 方案/动作:摸底现状(纯文档仓库、无代码、非 git)→ 给 4 方案(轻量骨架 / 标准可运行脚手架 / 重量工程化 / 边界优先),用户选「方案 2 标准」。
3. 方案/动作:逐项确认 → 英文命名 apps/services/docs;文档统一收进 docs/;后端先单聚合服务;design 两处合并;日志目录留根;后端命名 server + com.minsheng.platform。
4. 方案/动作:PowerShell 迁移目录 + 手工写脚手架文件(**不执行 pnpm install / mvn**)。
5. 验证:最终 tree 核对结构正确。

**产出物**
- 迁移 `需求调研/`、`市场分析/`、`agent-sdlc-standard/`、`ai-presets/`、`llm-wiki/` → `docs/`
- 合并根 `design/`(用例图 + session-*)→ `docs/design/`
- `apps/pc/`(React18+Vite+Electron)、`apps/mobile/`(uni-app Vue3)、`services/`(Spring Boot 3.5.3 单聚合 server)
- 根 `package.json`、`pnpm-workspace.yaml`、`README.md`、`.gitignore`、`.editorconfig`、`services/README.md`(7 域预留拆分)

**关键决策与理由**
- 英文命名:git/工具链友好
- 后端先单聚合 + 按域分包:先单体后拆
- `AI_DEV_LOG`/`PROJECT_LOG`/`AGENT_MEMORY.md` 留根:技能硬编码根路径

**经验与教训**
- 沙箱直连 npm/GitHub raw 被 TLS 拦截(见会话 2),uniapp 版本用已知稳定 build 3.0.0-4020920240930001
- uniapp 约定 pages.json/manifest.json/pages 放 src/ 下(初建目录位置放错,已修正)

**下一步**:用户本机 push(见会话 2);联网后照 `apps/mobile/README.md` 升级 uniapp 版本。

## 会话 2:绑定 GitHub + Gitee 双远程

**本次目标**:git init + 双远程 + 首次提交。

**过程记录**

1. 需求/问题:用户给 GitHub `jzywm/Livelihood-Platform` 与 Gitee `jia-ruihang/livelihood-platform`,要求绑定。
2. 方案/动作:探测环境(git 2.52 ✅、全局身份已配置 ✅、外发 HTTPS 被 schannel 拦 ❌)→ 确认 origin=GitHub、两仓库为空、身份用全局。
3. 方案/动作:`.gitignore` 补排除工具目录;`git init -b main` + 双 remote + 首次提交。
4. 验证:git status 干净、remote -v 正确、误纳工具目录检查为空、205 文件提交成功。

**产出物**
- 首次提交 `7c3c429 chore: init monorepo structure`(205 文件,17468 行)
- `.gitignore` 补排除 `.downloads/`、`.dsh/`、`.npm-cache*/`

**关键决策与理由**
- origin=GitHub、gitee=Gitee;分支 main;身份 jia ruihang <584431019@qq.com>

**经验与教训**
- 沙箱外发 HTTPS 被 schannel 拦(SEC_E_NO_CREDENTIALS)→ push 只能用户本机执行
- git quotePath 对中文文件名转义是正常显示,非真实文件名

**下一步**:用户本机执行 `git push -u origin main` + `git push -u gitee main`。

## 会话 3:高并发架构演进设计(非功能需求详设)

**本次目标**:讨论如何满足非功能需求(高并发),产出一份可评审、可编码的高并发架构演进设计详设。

**过程记录**

1. 需求/问题:用户提出「讨论一下如何满足非功能需求、实现高并发」,后续选定方向 A、追加「看看能不能再优化」。
2. 方案/动作:精读《非功能需求-信息安全与可靠性专项》§5(并发≥10 万/读≥15 万 QPS/写≥5 万 TPS/API P95≤500ms)+ 概要设计 §8(已含逐链路瓶颈表与零散演进点)+ 部署/逻辑架构 → 判断缺的不是概述,而是把 §8【待评审】落成执行级 + 收敛为一张路线图。
3. 方案/动作:给 4 方案(①详设独立成篇 ②路线图+ADR 先行 ③概要§8 内深化 ④图+精简文档),用户选**方案 1**。
4. 方案/动作:精读概要 §2.1 架构风格/§3.1 十项组件/§4.4~4.5 分表/§6.2~6.3 幂等与 MQ/§8.1~8.6/§9 里程碑/§11.1 → 落盘 `docs/design/高并发架构演进设计.md`(9 节)。
5. 方案/动作:用户追加「再优化」→ 增补 §6 差距分析与优化建议(10 个优化点,含 4 个 P0)。
6. 方案/动作:`产品设计文档.md` §9.1 交付物表加一行指针。
7. 验证:文档写入 + 指针编辑成功;容量数字/组件选型/里程碑与概要 §8、专项 v1.0 逐项对齐。

**产出物**
- `docs/design/高并发架构演进设计.md`(新建,9 节:演进路线图/数据层详设/异步削峰/限流熔断/容量压测/差距优化/ADR/待评审/验收)
- `docs/design/产品设计文档.md`(§9.1 交付物表 +1 行指针)

**关键决策与理由**
- 演进口径「M1 压测分层验证规划峰值 1.5x,M2 全量达到 10 万并发/15 万 QPS」——与「先试点、架构预留市级」一致(与专项 §5.4 字面有张力,待用户确认)
- 分片键与主键解耦:主键雪花 ID + 分片业务键/时间,禁雪花 ID 直接 hash 分片
- 公示数据快照化:红黑榜/信用分读多写少改版本化快照,消缓存击穿+一致性两难题
- 15 万读 QPS 里 ≥90% 必须被缓存/快照吸收,DB 只承受 1~2 万 QPS(缓存命中率=第一杠杆)

**经验与教训**
- 概要 §8 已含相当完整概述,详设价值在于把【待评审/建议初值】落成执行级 + 收敛零散演进点为路线图,而非重复概述
- 高并发地基(分表/分布式 ID/无状态/幂等/限流/可观测)M1 启动即做——「拆不动、补不起」的不能因试点流量小后置

**下一步**:用户确认「M1 压测口径」+ §8 的 9 个 TBD-* 待评审项;可选:把 §6 优化点提炼成评审议题清单 / archify 出交互式演进图。

## 会话 4:按产品设计文档微服务划分创建 services 子目录与说明文档

**本次目标**:依据《产品设计文档》的微服务划分,在 `services/` 下为每个微服务创建子目录 + `docs/` 说明文档,明确各服务功能与质量基线。

**过程记录**

1. 需求/问题:用户要求「根据产品设计文档中的微服务划分,在 services 目录下为每个微服务创建对应子目录;每个子目录下创建 docs 目录并生成该微服务说明文档(至少含 概述/背景条件/主要功能/非功能性需求)」。
2. 方案/动作:定位两份基线文档 `docs/design/产品设计文档.md`(PDD v1.1)与 `docs/需求调研/核心成果/产品需求文档.md`(PRD v2.0);先 grep 大纲定位,再分段精读 PDD §2.4 模块与服务映射、§5.1~5.13 服务功能设计、§6.4 接口清单、§8 非功能设计,以及 PRD §2 背景/§3.4 红线 R-01~R-16/§7 非功能需求。
3. 方案/动作:确定 13 个微服务(ACC/SETTLE/CRED/EMP/TICKET/TRACE/ASSIST/PROD/TRADE/DASH/PROFILE/AICORE/CIVIC),以服务代号小写为目录名;PowerShell 一次性创建 13×`docs/` 目录。
4. 方案/动作:每服务写 `docs/README.md`,统一五节模板(服务概述/背景条件/主要功能/非功能[通用基线+服务专项]/参考文档),逐条锚定 PDD/PRD 章节号。
5. 验证:glob 确认 13 篇 README 齐全;grep 校验每篇均含 4 个必需节(52 处匹配)。

**产出物**
- `services/{acc,settle,cred,emp,ticket,trace,assist,prod,trade,dash,profile,aicore,civic}/docs/README.md` 共 13 份(各约 4~6 KB)

**关键决策与理由**
- 目录名用服务代号小写(acc/settle/…):与 PDD 服务标识一一对应、可回溯;中文全名写在每篇标题与概述
- 文档统一五节模板 + 锚定 PDD/PRD 章节号:作为后续扩展开发的功能/质量基线
- 双语言边界随文档固化:ASSIST/AICORE = 独立 Python 服务(FastAPI + LangChain/模型编排),其余 Java 17 + Spring Boot 模块化单体;PROFILE 画像业务留 Java(算法子服务后置 Python 属 AICORE 扩展位)

**经验与教训**
- 设计文档(约 134 KB)先 grep 大纲定位章节再分段精读,避免整篇读入
- 非功能需求用「通用基线 + 服务专项」两层写法:通用基线(规模/性能/SLO/容灾/安全)全平台一致,专项按服务差异化(可用性 L1/L2/L3、红线、降级、缓存、幂等、脱敏)

**下一步**:按需补每服务接口清单(PDD §6.4)/实体字段(PDD §4.5)/启动指引;如需改目录命名规范(如 account-service 风格)再调整。

## 会话 5:根据页面清单与信息架构画页面流程图(archify)

**本次目标**:依据 `apps/docs/页面清单与信息架构.md`(89 页、三端合一 + RBAC)产出「关键用户路径」页面流程图。

**过程记录**

1. 需求/问题:用户要求「根据项目的页面清单和信息架构帮我画一下页面流程图」,附例:首页→商品详情→购物车→结算→下单成功,分支「未登录→登录页→返回继续」。
2. 方案/动作:先出 4 方案(Archify 交互式 HTML / Mermaid / ASCII / 分层多图)× 范围(关键路径 / 全量 89 页),用户选「方案 A Archify + 范围 A 关键路径」。
3. 方案/动作:加载 `archify` 技能、选 `workflow` 类型;读 schema + 示例(release-delivery / incident-response)确定字段形状。
4. 方案/动作:写候选 `docs/design/diagrams/pageflow-key-paths.json`;多轮 `validate --quality showcase` 迭代修复(节点重叠→列对齐、边穿节点→via 正交折线、标签碰撞→短标签+减宽)。
5. 方案/动作:`deliver` 生成 HTML;`visual-check` 检查视口;用户最终确认「3 = 保持现状」接受垂直溢出。

**产出物**
- `docs/design/diagrams/页面流程-关键用户路径.html`(640 KB,自包含交互式,缩放/搜索/高亮/明暗主题/导出 PNG·SVG)
- `docs/design/diagrams/pageflow-key-paths.json`(源,4.9 KB,可再编辑重渲染)
- visual-check 产物(4 PNG 截图 + contact sheet + json)

**关键决策与理由**
- 用 `workflow` 类型:页面流 = 用户旅程/流程(非 architecture/lifecycle)。
- 登录·实名拦截(未登录→登录→返回)改「卡片 + 引导视图」呈现:画回环边会跨 4 泳道穿线,得不偿失。
- 三工作台从 `login` 分流(登录按主角色落地,符合 IA),而非从 `home` 分流;`findshop` 泳道提到 purchase 之后,使 home→shops 只跨 1 泳道。

**经验与教训(可复用)**
- archify workflow 渲染器**固定 720px 画布**:列中心 x=88/220/300/430/500/625,列距 132/80/130/70/125px;**每泳道每列仅 1 节点、不可堆叠**;col1↔2(80px)与 col3↔4(70px)放不下 92px 默认节点。
- 中文标签宽 ≈ 6.5px×(2×CJK+ASCII)+13,节点宽需 ≥ 标签宽−6;长标签(如「首页·消费工作台」)拆 label+sublabel。
- **沙箱禁止命名管道 → archify 的 validate/deliver/visual-check 内部 spawn 子进程被 EPERM 拦**,须 `danger-full-access` 升级重试(本机唯一例外)。
- 深分支跨多泳道用**正交 via 折线**(`via:[[x,y],...]` 每段横/竖),避免 drop 斜线穿节点;`via` 路径必须正交否则 `artifact/orthogonal-arrows` 失败。
- visual-check:workflow 多泳道图宽高比 <1.55 不触发阅读器自适应宽度,宽屏等比放大→垂直溢出(scrollHeight 1800~1905 vs 视口 900~1320),属渲染器固有约束、非内容/布线错误。

**验证**
- `validate --quality showcase`:✅ 9/9 产物检查、composition 0 错误 0 警告。
- `deliver`:✅ spec SHA-256 `a677d881…`、artifact SHA-256 `b2c63c89…`。
- `visual-check`:exit 1(垂直溢出,原因见上)。

**下一步**:无阻塞;可选后续(用户已选保持现状)——若需一屏无滚动,可精简泳道 / 改 architecture 类型重绘。

## 会话 6:PC Web 页面 ASCII 线框图(单页 → 51 页 M1)

**本次目标**:根据项目文档(需求说明 + 页面清单 + 信息架构)绘制页面 ASCII 线框图;从单页扩展到「每个页面」。

**过程记录**

1. 需求/问题:用户要求「根据项目文档中与目标页面相关的需求说明以及页面清单与信息架构,绘制单个页面的 ASCII 线框图」,并给出商品详情页示例格式。
2. 方案/动作:定位需求依据(`32-功能列表` T-04、`20-T模块` T1 页面清单、PRD、`产品设计文档` §11.4)→ 判定目标页 = **商品透明详情页(T-04)**,先画单页线框(来源/批次/检测/信用/价格/溯源/投诉 + 采购/购买按钮)。
3. 需求/问题:用户追加「我想要每个页面的线框图」。
4. 方案/动作:给 4 方案(方案 A 全量落盘 / **方案 B MVP 聚焦 M1** / 方案 C 双端优先 / 方案 D 全量贴出),待选。
5. 需求/问题:用户指示「查看 `apps/docs/页面清单与信息架构`,来决定画哪些」。
6. 方案/动作:精读 `apps/docs/页面清单与信息架构.md`(IA v1.2,89 页 = G10+S25+M33+R21;M1=51/M2=32/M3=6)+ 设计规范 `01`(设计原则)/`02`(设计令牌)/`03`(组件交互)。
7. 需求/问题:用户回「2」= 选方案 B(MVP/M1 聚焦)。
8. 方案/动作:按 IA §6.1 的 M1 共 **51 页**绘制(初估 25~30 页经 IA 校准为 51),统一遵循 IA §1.2 全局导航框架(三端合一单 App + 白话导航 + 底部「问一问」悬浮球)+ 设计令牌/状态徽章规范;每页附功能点溯源 + 五词自检。
9. 验证:`grep '^### (G|S|M|R)-\d+'` 命中 **51 条**,与 IA §6.1 完全一致。

**产出物**(新建 `apps/docs/线框图/`)
- `README.md`(全局导航框架 + 线框模板 + 组件约定 + 索引)
- `01-全局公共区-M1.md`(G-01~G-10,10 页)
- `02-共享能力域-M1.md`(S-01~S-06/S-09~S-13/S-25,12 页)
- `03-经营能力域-M1.md`(M-01~M-10/M-12/M-18~M-21/M-28/M-30/M-33,18 页)
- `04-监管能力域-M1.md`(R-01~R-04/R-07/R-08/R-10~R-13/R-20,11 页)

**关键决策与理由**
- 目标页 = 商品透明详情页(T-04):示例即商品详情 + T-04 文档聚合信息最完整(来源/批次/检测/信用/价格/溯源/投诉)。
- 范围 = 方案 B(M1=51 页):用户「2」确认;M2/M3 只列清单不画。
- 页面权威口径 = `apps/docs/页面清单与信息架构.md`(89 页,比 §11.4 矩阵更细,含路由/模块/里程碑/功能点/可见角色)。
- 线框三端合一单 App(功能域路由 + RBAC),非三端分离;端降级为「场景工作台」。

**经验与教训**
- IA 文档(`apps/docs/页面清单与信息架构.md`)是页面唯一权威清单(89 页 + 里程碑分批 + 反查索引),画线框前应先读它,而非散落在各需求文档拼。
- 方案初估页数需用 IA §6.1/附录 A 校准(「25~30」实为 51)。
- 设计规范 `01~03` 提供可复用的线框约束:每页单主按钮、状态徽章统一语义、进度用时间轴、资金/危险操作二次确认、脱敏公示。

**下一步**:可选补 M2(32 页)/M3(6 页)线框;线框 → `archify` 可探索 HTML 原型 / 落地 `apps/web` 功能域路由 + RBAC 守卫。

## 会话 7:页面清单与信息架构(IA)+ UI 设计规范(本会话,系会话 5/6 的上游)

**本次目标**:基于产品功能与设计文档,产出并迭代「页面清单与信息架构」,再产出「UI 设计主题规范」,均存 `apps/docs/`。

**过程记录**

1. 需求/问题:用户要求「基于产品功能与设计文档,输出页面清单与信息架构,存到 apps/docs」。
   方案/动作:精读 README/PDD/PRD/`32-功能列表`(79 功能点)/用例图 → 按记忆规则给 4 方案(三端×模块×页面/模块优先/里程碑优先/+导航框架图),用户选方案 A。
   产出物:`apps/docs/页面清单与信息架构.md` v1.0(87 页,三端前缀路由 /consumer /merchant /supervision)。
   验证:79 功能点逐页回溯、附录页数核算并修正。

2. 需求/问题:用户反馈「用户主体同一人,按端定死限制使用」,要求重新评估按端组织 + 助手位置。
   方案/动作:给 3 方案(功能域路由+场景工作台/保留端路由+场景切换/彻底去端);用户补充「考虑学习难度、主打方便」→ 收敛为方案一 + 低学习难度四板斧(角色化默认工作台/白话导航/渐进式披露/助手兜底);助手选底部中心悬浮球。
   产出物:IA 升 v1.1(去端化功能域路由,84 页,助手底部中心)。
   验证:去重合并 3 处(首页/履历/钱包),87→84 核算一致。

3. 需求/问题:用户三点——①商户原材料定期拍照上传(含产品编号)+视觉核验国标 ②助手支持语音 ③查用例图补缺页。
   方案/动作:核对用例图 79 用例,发现 5 处缺页;用户拍板:新增 T-22 功能点(原材料上报+监管核验)、语音 ASR+TTS、补 3 页(劳务信用互评/司机信用/争议仲裁)。
   产出物:IA 升 v1.2(89 页:补 3 页 + T-22 两页 + J-14 语音)。
   验证:用例图↔功能点逐一核对、附录统计修正为 51/32/6=89。

4. 需求/问题:用户要求「生成 UI 主题方案(关键词/配色/字体/组件/图标),可直接作 UI 规范」。
   方案/动作:读品牌定位(`26`,已确认民生蓝+暖橙)→ 给 3 主题方向 + D 三端差异化,用户选方案 B(民生温暖·现代亲民)。
   产出物:`apps/docs/UI设计规范.md` v1.0(9 章:Design Tokens 配色/字体/组件/图标/端差异化/无障碍/五词对照)。
   验证:色值含 WCAG 对比度、关键功能专属视觉覆盖已确认范围(原材料上报/视觉核验/语音助手/劳务信用/司机信用/争议仲裁)。

**关键决策与理由**
- IA 从「按端定死」→「功能域路由 + 场景工作台 + 一人多角色叠加」:端降级为聚合首页,跨场景零切换
- 低学习难度四板斧:主线是「方便」,白话标签不暴露系统术语
- 助手改底部中心悬浮球 + 语音(ASR+TTS):导航做减法、助手做兜底,服务银发/司机
- T-22(原材料上报)/J-14(语音)为 79 功能点之外新增,标注「待走需求变更流程」(规格书 §10)

**经验与教训**
- 三端合一单 App(PDD F3)下,「端」应是能力/角色维度而非路由硬分区;当前仓库 apps/pc+mobile 是重构前状态,IA 是目标态
- 附录页数每次改动需重新核算(去重合并/新增页都影响计数)
- 本会话 IA 文档是后续并行会话(会话 5 流程图、会话 6 线框图)的唯一权威页面清单输入

**下一步**:UI 规范落地(设计令牌 CSS 变量 + AntD 主题定制 + 关键页高保真);T-22/J-14 需求变更登记

## 会话 8:前端设计规范与约束(apps/docs,README+01~06)

**本次目标**:根据项目定位与整体功能,产出一份前端设计规范与约束,存放在 `apps/docs` 目录。

**过程记录**

1. 需求/问题:用户要求「根据项目定位以及项目整体功能,帮我一份前端的设计规范和约束,存放在 apps/docs 目录下」。
2. 方案/动作:摸底项目(根 `README.md`/`DEVELOPMENT_CONSTRAINTS.md`/`docs/需求调研/README.md`/PDD §1.1/§2.4/§3/§11/`26-平台命名与品牌定位.md`)→ 按记忆规则先给 4 方案(①单文件 ②结构化文档集 ③文档+设计令牌源码 ④文档+自检门禁),用户选「方案 2 结构化文档集」+「新建 apps/docs」。
3. 方案/动作:新建 `apps/docs/`,产出 README 索引 + 6 规范文档(01 设计原则与品牌视觉 / 02 设计令牌 / 03 组件与交互 / 04 响应式与多端适配 / 05 前端工程与代码约束 / 06 安全与合规约束)。
4. 方案/动作:发现 `apps/docs/` 下已有并行会话产出《页面清单与信息架构.md》《UI设计规范.md》《线框图/》,将 IA 纳入 README 索引;发现落款日期误写 2026-09-07,核对系统时间(实际 2026-09-06)后修正 7 处。
5. 验证:`list_dir` 核对文件齐全;grep 确认 7 处日期已改 2026-09-06;文档反向锚定事实来源(26/PDD/DEVELOPMENT_CONSTRAINTS)。

**产出物**
- `apps/docs/README.md` + `apps/docs/01~06`(设计原则与品牌视觉 / 设计令牌 / 组件与交互 / 响应式与多端适配 / 前端工程与代码约束 / 安全与合规约束)

**关键决策与理由**
- 方案 2(结构化文档集):模块化、可独立评审演进,与现有 docs 分层风格一致。
- 存放 apps/docs:按用户指定;README 说明与根 `docs/` 关系。
- MUST/SHOULD/禁止分级沿用 `DEVELOPMENT_CONSTRAINTS.md`;色值/字阶「初版基线待品牌定稿校准」,命名分层 MUST(锁结构不锁数值)。

**经验与教训**
- 记录日期误写 2026-09-07,收工核对系统时间后修正(实际 2026-09-06)。
- **发现重叠**:本会话 02/03(设计令牌、组件交互)与并行会话 7 产出的 `apps/docs/UI设计规范.md` 部分重叠,后续需合并对齐为单一权威(避免两套令牌/组件规范并存)。

**下一步**:①与 `UI设计规范.md` 合并对齐设计令牌与组件规范(定唯一权威)②可选:令牌源码落地(colors.css/tokens.ts)③前端设计自检清单挂钩 commit-check。



## 会话 11:产品设计文档按微服务维度重构(PDD v1.0→v1.1)

> 时序说明:本会话产出 PDD v1.1 的「服务功能设计 13 微服务分类」,是会话 4(按微服务建 services 子目录)的依据;因与其余会话并行记录,序号排后。

**本次目标**:优化 `docs/design/产品设计文档.md` —— ① 整体结构优化;② 后端模块化分类统一按微服务维度(一个服务一个分类);③ 补功能详细说明(六段模板);④ 参考 PRD 优化。

**过程记录**

1. 需求/问题:用户要求「检查并优化产品设计文档结构;分类标准统一按后端微服务维度、粒度到每个微服务、每个功能按 功能名称/描述/前置条件/操作流程/后置状态/异常处理 描述;执行中不确定先询问」,后补充「可参考产品需求文档优化」。
2. 方案/动作:按 AGENT_MEMORY 规则先给 3 方案(A 新增功能说明章节 / B 功能层按服务重构 / C 全文按服务重构),用户选 **B**;再确认功能粒度选「约 50 核心功能」与范围选「全部 13 服务」。
3. 方案/动作:精读 PRD《产品需求文档.md》(1282 行 v2.1)+ `32-功能列表.md`(79 功能点)+ `14-功能结构图.md` 作功能口径来源。
4. 方案/动作:将 §5「核心流程设计」整体替换为 §5「服务功能设计」,按 13 微服务各一小节(ACC/SETTLE/CRED/EMP/TICKET/TRACE/ASSIST/PROD/TRADE/DASH/PROFILE/AICORE/CIVIC),55 个功能 × 六段模板 + 9 条核心流程状态机并入对应服务;§6.4 更名「按服务接口清单」逐服务映射;§0 版本历史 + v1.1。
5. 方案/动作:grep 全量定位 14 处旧 §5.x 交叉引用(§3/§7.4/§8.2.4/§9.1/§10/§11.1/§11.3)批量 edit 修正为新服务编号。
6. 验证:grep 确认 13 服务小节齐全、51 处功能/流程小节结构完整、无遗留旧引用(除历史版本记录);顶层结构正确,与并发会话 3/4/5/6 的写入共存无冲突。

**产出物**
- `docs/design/产品设计文档.md`(895 → 1630 行,v1.0 → v1.1)

**关键决策与理由**
- 方案 B:功能/流程/接口统一按服务重构,横向章节(技术/NFR/安全)不动——一次到位、改动中等、不丢横向内容
- 功能粒度 55 个(≈50 核心功能),口径对齐 PRD §5(P-01~K-06)与 `32` 功能点
- 六段模板操作流程固定 4 步(用户点击 → 系统展示 → 用户输入 → 系统返回)

**经验与教训**
- 大文档重构:先精确重读待替换区,再「一次大 edit 替换」,避免分片编辑导致结构断裂
- 交叉引用修正:grep 定位 §5.x 后注意区分「专项 §5.x」外部非功能文档引用不可改
- 多会话并发写同一日志文件时 edit 报「file changed since read」→ 用「临时文件 + pwsh 追加」绕过竞争

**下一步**:可选重构 §6.4 接口清单使其与 §5 物理同序(当前靠顶部映射表对齐);历史版本记录(v0.6/v0.8)旧 §5.x 编号未回改,按历史快照保留。

## 会话 9:建立全局开发约束规范

> 时序说明:本会话与并行会话(高并发/微服务/IA/线框图/前端规范等)并发记录,序号排后,内容独立。

**本次目标**:确立约束开发工作的全局规范(技术栈/代码风格/测试/提交分支/评审/安全/性能/文档日志)。

**过程记录**

1. 需求/问题:用户要求「确定全局开发约束规范,过程任何不确定事项由用户决定」。
2. 方案/动作:摸底现状(`AGENT_MEMORY.md` 已有 3 规则;`agent-sdlc-standard` 已有五层门禁 G0~G4;`.editorconfig` 已有基础格式)→ 给 3 方案(A 扩展记忆文件 / B 独立文件+记忆指针 / C 文档+工具校验)+ 询问范围与性质。
3. 方案/动作:用户选定 —— 方案 B(独立文件+指针)、覆盖 8 类、MUST/SHOULD 分级;7 项细节拍板(Conventional Commits 中文描述 / 标识符英文+注释中文 / ESLint·Prettier SHOULD 后续引入 / 分支类型前缀 / 性能只给通用原则 / 后端一并写入预留 / Java 阿里手册)。
4. 方案/动作:起草 `DEVELOPMENT_CONSTRAINTS.md`(8 大类 MUST/SHOULD,详细流程引用 agent-sdlc-standard)+ `AGENT_MEMORY.md` 追加「规则四」指针。
5. 验证:两文件写入成功、规则四读回核对无误。

**产出物**
- `DEVELOPMENT_CONSTRAINTS.md`(8 类约束:MUST/SHOULD 分级)
- `AGENT_MEMORY.md` 规则四「开发遵循 DEVELOPMENT_CONSTRAINTS.md」

**关键决策与理由**
- 独立文件 + 记忆指针:约束集中可版本化,记忆只留引用
- 详细门禁不重写、引用 agent-sdlc-standard:避免两套标准打架
- MUST/SHOULD 分级:红线强制、其余建议

**经验与教训**
- 起草前先摸清仓库已有标准(agent-sdlc-standard 已覆盖 Git/安全/评审),新规范只保留「每次开发都遵守」的核心约束,避免重复造轮子

**下一步**:落地提交门禁(见会话 10)。

## 会话 10:落地提交代码检查门禁(commit-check 技能 + git hooks)

**本次目标**:提交前强制检查 diff 是否遵循约束,违规阻断提交、逐项列位置/条款/修复建议,禁止跳过。

**过程记录**

1. 需求/问题:用户要求「创建提交代码 Skill:提交前强制检查 diff 遵循 DEVELOPMENT_CONSTRAINTS.md 与 agent-sdlc-standard 提交要求,违规阻断并逐项列出位置/条款/修复建议,禁止跳过」。
2. 方案/动作:摸底(无 git hook、main 分支、skill frontmatter 格式、agent-sdlc 01/06 提交要求)→ 给 3 方案(A 纯 AI Skill / B Skill+git hook / C 全套 G0 门禁)+ 发现规范冲突(分支命名是否带工单号)。
3. 方案/动作:用户选定 —— 方案 B(Skill+hook 双保险)+ 分支命名带工单号(以 agent-sdlc-standard 为准)。
4. 方案/动作:写 `.dsh/skills/commit-check/SKILL.md`(A–F 六类语义检查)+ `.githooks/pre-commit`(冲突标记/行尾空白/>1MB/私钥密钥/.env)+ `.githooks/commit-msg`(Conventional Commits)+ `DEVELOPMENT_CONSTRAINTS.md` §4 修正(分支带工单号 / [AI] 标记 / 门禁条款)。
5. 方案/动作:`git config core.hooksPath .githooks` 启用。
6. 验证:hooksPath 设置成功、`git rev-parse --git-path hooks`→`.githooks`、脚本 LF 行尾(无 CRLF 破坏 shebang)、技能已注册(出现在 available_skills);**沙箱无法端到端跑 bash hook(命名管道 Win32 error 5 被拦),待用户本机 git commit 实测**。

**产出物**
- `.dsh/skills/commit-check/SKILL.md`(提交前语义级强制检查)
- `.githooks/pre-commit`、`.githooks/commit-msg`、`.githooks/README.md`(物理门禁)
- `DEVELOPMENT_CONSTRAINTS.md` §4(分支带工单号 + [AI] 标记 + 门禁条款)
- `git config core.hooksPath .githooks`(本机启用)

**关键决策与理由**
- 方案 B:AI 语义深检 + hook 物理阻断双保险,无额外依赖(方案 C 需装 Python/gitleaks)
- hook 做确定性检查(粗筛)、skill 做语义判断(精判),"且"关系
- 分支带工单号:以 agent-sdlc-standard 为准,同步修正约束文档
- 诚实说明物理上限:`--no-verify` 可绕过本地 hook,真正不可跳过由 CI G2 兜底

**经验与教训**
- 沙箱禁止 Git Bash 创建命名管道(signal pipe Win32 error 5)→ 无法在本环境端到端执行 hook,只能验证 hooksPath/行尾/注册
- hook 脚本必须 LF 行尾,CRLF 会破坏 `#!/bin/sh` shebang
- 多会话并发写同一日志文件时 edit 报「file changed since read」→ 用「临时文件 + pwsh 追加」绕过竞争(本会话沿用)

**下一步**:用户本机 git commit 实测 hook;新克隆者执行 `git config core.hooksPath .githooks`。