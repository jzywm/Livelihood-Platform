# AI 开发日志 · 2026-09-14

## 会话 1:网关微服务方案探讨(explore 模式)

- **主题**:网关微服务先做 + 全项目网关内容写入该微服务 + 优先级提高 + 实现方案设计
- **需求/问题**:用户「/openspec-explore 我现在想要先做一下网关的微服务,要求将项目中所有关于网关的内容全都写入这个微服务:1.将网关微服务的优先级提高 2.设计一下网关微服务的实现方案」
- **方案/动作**:grep 全量定位「网关」三义并存 + 读 PDD §5.16/高并发 §7/SSOT §2.15/GATEWAY README/acc 代码锚点(AuthFilter/JwtCodec/TraceIds/AuthContext/ErrorCode)→ 按记忆规则给 4 方案(A 轻量骨架先行/B 标准网关先行接管/C 重量配套全落地/D 仅口径),用户裁决:**B + K-02 保持 AICORE 边界 + 移除 ACC 内嵌 Filter(单一权威)**
- **产出物**:方案 B 定稿设计(六决策 D1~D6:里程碑 M1 后期→M1 初期交付即接管/网关唯一鉴权点四头透传/兜底改 503 快速失败/R4J M1 熔断/初期静态路由/运维 openapi 随交付补)
- **验证**:用户逐项裁决(ask_user_question 3 问全录)

## 会话 2:OpenSpec 变更 implement-gateway-service 创建

- **需求/问题**:用户确认「创建」OpenSpec 变更捕获方案
- **方案/动作**:`openspec new change "implement-gateway-service"` → status/instructions 驱动,按依赖序创建 4 工件(proposal/specs/design/tasks);核实主 specs 为空 → 新能力 `platform-gateway`;spec 文风对齐 add-m1-core-capabilities(英文需求名 + #### Scenario 四井号)
- **产出物**:`openspec/changes/implement-gateway-service/{proposal.md, specs/platform-gateway/spec.md, design.md, tasks.md}`(8 需求 18 场景;D1~D9 决策;24 任务)
- **验证**:`openspec validate` 通过 + status 4/4 done

## 会话 3:变更深化(update-change)

- **需求/问题**:用户「/openspec-propose 完善 getway 微服务的各个功能的实现」→ 裁决「A 深化现有变更」
- **方案/动作**:以代码锚点为准补实现细节——核实 acc 控制器路径无 /api/v1 前缀(→路径重写)、错误码无 2004/5003(→D10 增补)、TraceIds=UUID 兜底、AuthContext 四字段(→四头透传);design 新增「功能实现设计」F1~F8 + D10/D11,tasks 24→32(类+测试粒度),proposal Impact 一行微调
- **产出物**:design.md(F1 路由重写/F2 白名单精确三路径/F3 限流 key 与初值/F4 R4J 参数/F5 traceId/F6 灰度/F7 运维/F8 配置总表)、tasks.md(12 组 32 任务)、proposal.md
- **验证**:`openspec validate` 通过、状态 4/4

## 会话 4:superpowers TDD 实施网关代码(未完成,明日继续)

- **需求/问题**:用户「使用 superpowers 完整流程,基于 openspec/changes/implement-gateway-service/ 目录下文档进行微服务 gateway 代码编写:1.研读规范、拆解任务 2.完成代码自查与评审」
- **方案/动作**:加载 using-superpowers/openspec-apply-change/TDD/requesting-code-review 四技能;范围=任务组 2~9(gateway 代码 + _common 错误码),组 1/10/11/12 留后续;创建 goal 跟踪;全程 TDD(先测试看 RED 再实现);环境:Java 17.0.2 + Maven 3.8.1,本地仓库在 `D:\soft\maven_3.8.1\pro`(settings.xml 自定义 + 阿里云镜像),`_common` 需 `mvn install` 供 gateway 引用
- **产出物(已完成)**:
  - 2.1 `services/gateway/pom.xml`(无 parent 惯例 + BOM import 锁 Boot 3.5.0/SC 2025.0.0;依赖解析 BUILD SUCCESS)
  - 2.2 `GatewayApplication`/`GatewayProperties`/`GatewayConfiguration`/`application.yml`(路由+白名单+限流初值+internal-paths)/`.env.example`;actuator health UP 验证过(mock-maker-subclass 惯例照抄 acc)
  - 3.1 `_common` ErrorCode **2004 RATE_LIMITED/5003 GATEWAY_UNAVAILABLE** + openapi.yaml 同步 + `mvn install`(_common 全绿)
  - 3.2/3.3 `TraceIdFilter`(UUID 兜底)+ `GatewayErrorWebExceptionHandler`(2001/2004/5002/5003/3006/5000 → Envelope 五字段,替代 whitelabel)
  - 4.1~4.4 `ClaimsJson`(手写 JSON 解析)+ `JwtVerifier`(手写 HS256)+ `RevocationStore`(Redis/内存双实现)+ `RedisRevocationStore`/`InMemoryRevocationStore` + `JwtAuthFilter`(白名单/多值头取首/四头透传 X-User-Id/Role/Mfa/Jti/保留 Authorization/吊销 fail-closed 503)
  - 5.1~5.3 `RateLimiter` 端口 + `RedisLuaRateLimiter`(lua/rate-limit.lua)+ `InMemoryRateLimiter` + `Tier`(AI>FUNDS>WRITE>READ)+ `RateLimitFilter`(三级 key:rl:ip/rl:acc:{tier}:{sub}/rl:api:{tier}:{method}:{path},AI 不做接口级全局桶防误伤,429+2004,fail-closed 503+5003)
  - 6.1~6.3 `TimeoutFilter`(metadata.timeout)+ `InternalPathGuardFilter`(order 5,internal/callback 404+3006)+ `GatewayRoutingIntegrationTest`(WireMock 真实 SCG 路由:路径重写/404/超时 503)——TimeoutFilterTest 3/3 绿;集成测试最后一步待修
- **验证(已绿)**:ClaimsJson/JwtVerifier/RevocationStore/LettuceGatewayRedisOps/JwtAuthFilter/TraceId/ErrorHandler/Properties/Application/RateLimit 全套单测 + TimeoutFilterTest 均过;集成测试 3/4 绿(仅 internal 守卫用例在改法)
- **关键决策与理由**:① 存储模式 `gateway.store=redis|memory` 双实现、Redis 由网关自建(排除 starter 自动装配,memory 模式 health UP);② Redis 故障 fail-closed(503+5003)不静默放行;③ AI 限流只做 IP+账号级(10/min/账号口径是"按账号",全局接口桶会误伤多账号流量);④ 路由 URI 用 `${GATEWAY_ACC_URI:...}` 占位符注入(测试/部署可覆盖)
- **经验与教训(今天踩的坑,详见 README 复刻要点)**:
  1. JLS 15.25:三元 `cond ? Double : Long` 因两操作数可拆箱做数值提升,整数被吞成 Double——用 if 分支返回(ClaimsJson 解析 bug,靠独立复现+字节码定位)
  2. SCG 4.3 `PredicateDefinition` **没有 negate 属性**,yml 负向谓词被静默忽略且 `args.patterns` 列表会摊平为 `patterns.0` 键导致路由全挂 → 内部接口不可达改用自研 InternalPathGuardFilter
  3. SCG 4.2+ 路由键位迁移 `spring.cloud.gateway.server.webflux.routes`;`Route.async()` 存在但 predicate 是 `org.springframework.cloud.gateway.handler.AsyncPredicate`(不在 Route 内);`Route.builder()` 是同步版
  4. `@DynamicPropertySource` 用索引键(`routes[0].uri`)与 yml 列表跨源合并会丢字段触发 SCG 校验失败 → 用自定义占位符属性注入
  5. WireMock 静态 `stubFor` 走默认 admin 端口 8080,必须在类加载期(static 块)先 start,且用实例 API(`downstream.stubFor`)最稳
  6. `WebTestClient.bindToWebHandler` 链式测 GlobalFilter:手工 `WebHandler chained = ex -> filter.filter(ex, handler::handle)` + `onErrorResume(e -> errorHandler.handle(exchange, e))`,不经 WebHttpHandlerBuilder(HttpHandler/WebFilter 适配两坑)
  7. JDK17 + Mockito inline 自附加失败 → `test/resources/mockito-extensions/org.mockito.plugins.MockMaker` 写 `mock-maker-subclass`(acc 惯例)
- **下一步(断点,明日从这里继续)**:
  1. 修编译断点:GatewayProperties record 新增第 4 组件 `internalPaths` 后,`GatewayPropertiesTest`(3 处 `new GatewayProperties(...)`)与 `RateLimitFilterTest`(props 工厂)需补第 4 参数;删除临时调试文件 `services/gateway/src/test/java/com/msz/gateway/RouteDebugTest.java`
  2. 跑全量 `mvn -f services/gateway/pom.xml test` 验证 6.x 集成测试(InternalPathGuardFilter 已实现未验证)全绿 → 勾选 tasks 6.1~6.3
  3. 任务 7.1:R4J 熔断(default-filters CircuitBreaker + forward:/gateway-fallback 兜底 503+5002,minimumNumberOfCalls 须显式配)
  4. 任务 8.1~8.3:GET /gateway/health、/gateway/routes + docs/openapi.yaml
  5. 任务 9.1:jacoco 行≥80/分支≥75 + `mvn verify` 全绿 → 9.2 commit-check 门禁
  6. 自查 + 派遣 code-review 子代理评审 + 修复 → 勾选 tasks.md → 汇报(组 1 文档口径/组 10 ACC 瘦身/组 11 部署/组 12 演练仍留后续)

## 会话 5:ACC 微服务实现闭环收尾(并行会话)

- **需求/问题**:用户「记录今天」;本会话为 ACC 工作线的收尾记录(与网关会话 1~4 并行)。
- **方案/动作**:ACC(implement-acc-service)今日完成——S5 接口层(fresh 实现者 334080c/bd0ba39:16+1 契约/矩阵/WireMock;fix ConstraintViolationException→1002)+ S6 控制器自办(JaCoCo 门禁 e15c335、29 分支测试 12fe9ea、§11/EVIDENCE fbe5160、22/22 勾销 386d2af)→ 最终整体评审(ec612ddc)With fixes → 修复波(a68fa578,3c5608d:对账槽位/captcha 原子/通道 SUSPENDED/注销拒访/卡号边界)→ re-review(99cc1f67)F1~F5 全 ADDRESSED → 文档收口(eb42c38)。
- **产出物**:commits 334080c..eb42c38;**288 例全绿 + JaCoCo 门禁通过(整合树 `mvn verify` EXIT=0)**;SDD ledger 12 项裁决 + 29 项 deferred triage;EVIDENCE.md(伞变更 2.1/2.2 勾销证据);test-plan §11.1~11.3。
- **验证**:最终验证 288/288 + JaCoCo「All coverage checks have been met」;S1~S5 评审全 Approve、最终评审 re-review 全 ADDRESSED。
- **关键决策**:I-2/I-3(role 恒 CONSUMER/mobile_hash 判重死路径)豁免本分支→伞变更 er.md v1.2+V3;I-1 TX/I-8 装配三件套/I-9 月表预建(2026-08 硬截止)移交伞变更验收;SDD 工作区保留存档。
- **待用户拍板**:分支整合三选(推荐本地合并 main→伞变更 2.1/2.2 勾销→archive);注意与网关会话共用工作树——网关线有未提交改动(_common ErrorCode 2004/5003 增码、githooks/标准文档)与其提交 2a0ee91 缺 `[AI]` 前缀(commit-check 违规,待其补正)。
- **经验**:① 并行会话共享日志文件时「只追加不篡改」,且**不要代对方提交其未提交改动**;② 收工记录前先 `git status` 查他人在途改动。
