# 2026-09-05 · 会话 1：大文件重复读取痛点 → 选型并落地 dsh-code-intel（DSH 工具链）

## 会话信息

- 主题：项目规模太大，每次开新会话 Agent 都要重读一遍大文件 → 找插件避免重复读取（索引 / 缓存 / 按需加载 / 增量读取），并落地到 DSH
- 本次目标：找到并确认一款「避免每次新会话重复读大文件」的插件，给出可执行用法
- 日期：2026-09-05（文件名即日期）
- 环境：工作区 `D:\progrom`；DSH 检出 `D:\soft\deepseekharness\deepseek-harness`；个人根 `<DSH_HOME>=C:\Users\贾锐航\.dsh`

## 过程记录（可复现四要素）

### 1. 需求
- 用户原文：项目规模太大，每次开启新会话时 Agent 都需要重新读取一遍项目中的大文件；找一个能避免重复读大文件的插件（索引 / 缓存 / 按需加载 / 增量读取）

### 2. 方案 / 动作（选型）
- 先 `find_dsh_plugin` 搜（两次均 `Error: fetch failed`）→ 改 `web_search` 检索 GitHub/npm 生态 + 交叉核对仓库
- 候选对比：`dsh-code-intel`（符号索引 + 混合检索，⭐ 首选）、`dsh-context-milvus`（向量索引，需自建 Milvus）、`dsh-token-optimizer`（增量 diff + 缓存，补充）、`dsh-code-index`（代码库索引，中文资料多）
- 推荐结论：首选 `dsh-code-intel`（最贴「索引 + 按需加载」、无需额外中间件）；milvus 需要额外服务、token-optimizer 偏增量 diff

### 3. 方案 / 动作（用法落地）
- 直连 `raw.githubusercontent.com` 被沙箱 TLS 拦截（Invoke-WebRequest 连接关闭 / curl schannel 无凭据 / node 证书校验失败）→ 改走 npm registry 镜像 `registry.npmmirror.com`
- `npm view dsh-code-intel`（`--cache D:\progrom\.npm-cache` 解决全局 cache EPERM）拿到 packument（v0.1.3，`dsh.bundle.patch=cordis.patch.yml`）+ 完整 README
- 发现插件**已装于 web profile**：读 `C:\Users\贾锐航\.dsh\profiles\web\package.json`（`dsh-code-intel@^0.1.3` 在 `dsh.profile.bundles`）与本地 `node_modules\dsh-code-intel\dist\index.js`（工具 = `code_search` / `code_outline`，默认配置，索引生命周期）
- 配置位置结论：`C:\Users\贾锐航\.dsh\profiles\web\cordis.patch.yml`（个人 patch 层，追加 `- id: code-intel / config: {...}`）

### 4. 产出物
- 会话内交付（无落盘文件）：插件选型结论 + dsh-code-intel 完整用法（安装命令 / 两工具 / 索引生命周期 / 配置 YAML / 卸载）
- 关键认知：dsh-code-intel = 符号级索引 + 词法/混合检索；索引 SQLite 存工作区 `.dsh/code-index/`、`fs/observed` + Chokidar 增量更新；首次 `code_search`/目录 `code_outline` 触发后台 `code-index` job；默认词法模式开箱即用

### 5. 验证
- `code_outline` 实测 `.downloads/dsh-market-src/dsh-market-main/src/registry.ts` → 返回 interface/function 符号清单 + 行号区间（单文件不依赖全量索引，证明默认配置已生效）
- `npm view` 确认 dsh-code-intel@0.1.3 metadata（engines node ^22.19||>=24；peer 依赖 cordis/dsh-*；`dsh.bundle.patch` 存在）
- 本会话工具集内已见 `code_search` / `code_outline` 注册（插件 active）

## 关键决策与理由

- 首选 `dsh-code-intel` 而非 `dsh-context-milvus`：前者无需额外中间件、索引落在工作区 `.dsh/code-index/` 跨会话复用，直接命中「避免重读大文件」；后者语义检索更强但需自建 Milvus，上手成本高
- 安装优先 Release tarball 预构建（`dsh plugin --profile web add <release .tgz URL>`）→ 免 pnpm 10+ 的 build 白名单；README 自述 npm 未 live，但 npmmirror 实测已有 0.1.3

## 经验与教训

- **沙箱网络边界（可复用）**：`find_dsh_plugin` / `raw.githubusercontent.com` 直连在当前沙箱不可达（TLS 拦截）；但 npm 走 `registry.npmmirror.com` 镜像可达，写全局 npm-cache 被 EPERM 拒绝 → `--cache` 指向工作区目录解决；`web_search` 是唯一稳定外网工具
- **插件「已装未用」盲区**：DSH 插件装在 `<DSH_HOME>\profiles\<profile>\node_modules`，是否生效由 profile `package.json` 的 `dsh.profile.bundles` 决定——用户此前不知已装，排查时先看 bundle 列表与 `dist` 源码最快
- 插件只「提供并推荐」code_search/code_outline，不强制禁止 `read` → 稳定不重读大文件还需 Agent 行为配合（系统提示已注入优先用索引的指引）

## 下一步

- 可选：给 dsh-code-intel 写显式配置（`cordis.patch.yml` 追加 code-intel config）；或把「查代码优先 code_search/code_outline」固化为常驻规则
- 平台主线不变（见 README 下一步）：概要设计 v0.6 评审升 v1.0（同步需求 v1.8）→ 数据库设计 → PC Web 原型

---
> 备注：2026-09-04 无日志档（本会话不掌握 09-04 活动）；本次为 09-05 唯一会话。
