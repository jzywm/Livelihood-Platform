# AI 开发日志 · 2026-09-15

## 会话 5:网关收尾——评审修复 + 文档口径同步 + Gate B(承接 2026-09-14 断点)

- **主题**:按用户"依次完成"指令推进 ①文档口径同步 ②Gate B(评审 Important)③设计工件措辞同步 ④ACC 瘦身
- **需求/问题**:用户「继续按照 superpowers 的流程编写网关微服务的代码」→ 完成后「按照顺序依次完成」(指 2026-09-14 汇报中列出的 4 项下一步)
- **方案/动作与产出**:
  1. **断点修复**:`GatewayProperties.internalPaths` 第 4 组件适配(测试构造)+ 删除临时调试测试 → 全量测试复绿;
  2. **任务 7.1 熔断**:`application.yml` 加 R4J 配置(时间窗 10s/失败率 50%/半开 5/恢复 12s/**minimumNumberOfCalls=4**) + `GatewayFallbackController`(503+5002,`X-Gateway-Fallback` 标记)+ `GatewayCircuitBreakerIntegrationTest`(WireMock `CONNECTION_RESET_BY_PEER` 故障注入 → 熔断打开 → 半开恢复);
  3. **任务 8.x 运维接口**:`GatewayOpsController`(`/gateway/health`、`/gateway/routes` 含 uri 脱敏)+ `docs/openapi.yaml`(js-yaml 校验通过);
  4. **任务 9.1**:`mvn verify` 全绿;jacoco 行 94.1% / 分支 84.6%(门禁 80/75);
  5. **自查发现并修 4 项**:XFF 伪造绕过 IP 限流(改取链尾)、Redis 故障 fail-closed 被熔断转成 5002(过滤器自写 5003 短路)、Redis 模式装配缺陷(排除 SCG/Spring Data 冲突自动装配 + 命令/连接超时 2s/1s)、**jar 无主清单**(补 `spring-boot-maven-plugin` repackage → 48.3MB 可执行包);
  6. **代码评审**(子代理只读评审,报告 3 Critical + 16 Important + Minors):**Gate A 全修**——C1 白名单路径不剥离伪造 `X-User-*`(新增 `RequestSanitizerFilter` order -20 全路径剥离 + 端到端断言下游收不到)、C2 路径混淆绕过 internal 守卫(`RequestPaths.canonicalize` 拒绝 `/./ // ../ %2e` + 段边界匹配)、C3 文档口径同步(见下);
  7. **Gate B 修复**:I3 熔断实例按路由命名、I4 换 `spring-cloud-starter-gateway-server-webflux`、I5 可执行打包、I6 Redis AUTH(用户名/密码)、I8 运维接口网段白名单(`gateway.ops.allowed-networks`,默认回环+私有网段,非内网 404)+ 白名单移除无效 `/gateway/` 条目 + openapi 错误码、I9 JWT 硬校验 `alg=HS256`、I10 接口级限流 key 路径变量归一 `{id}`、I11 内存限流器 `compute` 原子化(+16 线程并发用例)、I13 XFF 信任开关 `trust-forwarded-for`、I14 traceId 格式白名单、I16 超时值域非法回退默认 + Minor(降级端点外部直访 404、结构化日志、`.env.example` 补全、yml 注释纠正);
  8. **任务组 1 文档口径同步**(C3,9 处):PDD **v1.16**(§2.4/§3.1/§5 引言/§5.16/§6.2/§6.4 + 版本史 v1.16 行)、高并发 **v0.7**(§1.2/§1.3/§1.4/§4.1/§7/ADR-8/§10)、SSOT **v1.7**(§1 表/§2.15/C14 变更记录/§5 命名)、DEVELOPMENT_CONSTRAINTS §1、根 README(3 处)、部署图 01/02/03、**GATEWAY README v1.1**(§6 实现与部署要点新增)、待评审汇总(G-01/G-02 ☑ + D-15)、`add-m1-core-capabilities/tasks.md §9` 标注作废/承接、终审报告遗留③;
  9. **任务 11.x 部署件**:`deploy/Dockerfile`(多阶段构建 + HEALTHCHECK)、`deploy/docker-compose.yml`(redis + 网关双实例 + Nginx,YAML 校验通过)、`deploy/nginx-gateway.conf.example`(`/gateway/**` 不对外 + XFF 追加 + upstream 双实例)、`deploy/README.md`(切换 6 步 / 13 项验证清单 / 回滚 / 监控 / 已知约束);
  10. **任务组 3 设计工件同步**(评审 I1/I2/F6/F7 + 新增决策):spec.md(限流改**固定窗口计数** + 路径变量不可绕过 + Redis fail-closed + 运维接口网段要求)、design.md(D5 Filter 链 -20/-10/0/5/10/100、F3 算法与 key 口径、F6 weight 为预留、F7 访问控制、**F8 配置总表与实现对齐**、新增 **D12 客户端错误短路** / **D13 入口净化与内部守卫** / **D14 熔断实例与运维访问控制**、Migration Plan 阶段 1/2 标记完成 + 回滚口径改 503)。
- **验证**:`mvn -f services/gateway/pom.xml verify` → **132 用例 0 失败 BUILD SUCCESS**、jacoco 行 94.1%/分支 84.6%;jar 冒烟(health UP / routes / 401 / internal 401 / fallback 外部 404 / captcha 503);`openspec validate implement-gateway-service` 通过;openapi.yaml 与 docker-compose.yml js-yaml 校验通过;现行口径 grep 残留检查(仅剩版本史/变更记录/显式"作废"标注)
- **关键决策与理由**:① 评审 C1/C2 与我的自查一致 → 立即修并加端到端断言;② 运维接口不经网关过滤器链(Controller 直连)→ 访问控制独立声明(网段白名单)而非白名单配置;③ 熔断实例按路由命名(避免单实例劣化拖垮全路由);④ ops 配置拆独立类 `GatewayOpsProperties`,避免改动 `GatewayProperties` 既有 4 组件与 10 处测试构造。
- **经验与教训**:① **Spring Boot record 配置类只能有一个构造器**——加"兼容便利构造器"会导致整个上下文启动失败(本次 11 个上下文测试报错);要兼容就拆独立 `@ConfigurationProperties` 类;② YAML 合并编辑易留**重复键**(`filters:` 出现两次 → 解析失败),改路由段后应校验 YAML;③ `InternalPathGuardFilter` 的判定基准是「改写前路径」——路由 `RewritePath` 实际先于 order 5/10 执行(实测),故用 `RequestPaths` 落盘原始路径;**SCG CircuitBreaker 回退时 `GATEWAY_REQUEST_URL_ATTR` 已被改写**,判定"内部回退"须查 `CIRCUITBREAKER_EXECUTION_EXCEPTION_ATTR`;④ 评审代理与实现并行会互相干扰(报告记录了快照差异),大规模评审前宜冻结修订。
- **下一步(未完成,按序)**:
  1. **任务组 10 ACC 瘦身**(评审 I7 一并落地):删除 ACC `AuthFilter`/`RevocationStore`/`NoopRevocationStore` 与装配 → 新增 `TrustedHeaderAuthFilter`(`X-User-*` → `AuthContext`,缺失即 401 fail-closed)→ **迁移 5 个测试文件的鉴权断言**(`ErrorMatrixTest`/`ContractTest`/`WireMockChannelTest`/`AuthMatrixTest`/`AuthFilterTest` 共约 40 处 `Authorization` 头改为身份头或断言 401)→ ACC `mvn verify` 全绿;同时实现**吊销写入方契约**(登出/踢下线 `SET revoked:jti:{jti} 1 PX <剩余有效期>`);
  2. **任务 11.1 运行验证**:有 Docker 的环境跑 `docker compose -f services/gateway/deploy/docker-compose.yml up -d` 验证双实例 + 路径重写;
  3. **任务组 12 验收演练**:13 项验证清单逐项留痕(含故障注入:单实例宕机/全实例不可用 503);
  4. Gate C 剩余评测项:I9 的 `sub` 必填与 `exp` 上界(需策略确认)、I12 的端到端 429 用例、`rate-limit.lua` 独立测试;
  5. 提交:走 `commit-check` 门禁(注意工作区另有非本次变更的既有改动,提交粒度需拆分)。

## 会话 6:目标轮次 1 —— 第 4 阶段(ACC 瘦身 + 验收演练)完成

- **主题**:完成"按顺序依次完成"的最后阶段:任务组 10(ACC 瘦身,网关唯一鉴权点落地)+ 任务组 12(验收演练)
- **需求/问题**:目标轮次要求继续推进剩余阶段并验证
- **方案/动作与产出**:
  1. **任务 10.1 ACC 瘦身**(评审 I7 配套):新增 `TrustedHeaderAuthFilter`(读 `X-User-Id/Role/Mfa/Jti` → `AuthContext`,**缺/空白身份头 → 401+2001 fail-closed**,白名单与内部接口放行);删除 `AuthFilter`/`RevocationStore`/`NoopRevocationStore` 及装配(保留 `JwtCodec` 签发、`AuthContext`、`FundsGuard` 2002、回调 Hmac);新增 `TrustedHeaderAuthFilterTest`(5 用例);**迁移 4 个测试文件鉴权断言**(`ErrorMatrixTest`/`ContractTest`/`WireMockChannelTest`/`AuthMatrixTest`,新增 `TestIdentity` 工具以 `RequestPostProcessor` 注入身份头;篡改/过期 token 用例改为「空白身份头 → 401」);**ACC 251 用例 0 失败 BUILD SUCCESS**;
  2. **I7 吊销写入方契约**(design.md D3 补):`SET revoked:jti:{jti} 1 PX <token 剩余有效期>` 由签发侧(账户域登录/登出)写入,网关只读;ACC 当前无登出接口 → 新增接口属**接口清单变更**(需同步 openapi 权威源与前端 api-client),**不臆造接口**,契约先固化、实现随登录/登出功能交付;
  3. **任务 12.1 五用例演练(真机实证)**:网关 fat jar(18081,`store=memory`)+ Node 下游桩(8080,`deploy/drill/downstream-stub.cjs`)——13 项清单全绿:health 200 UP / routes 200 / 无 token 401 / **合法 token 200 且路径重写 `/api/v1/acc/me`→`/acc/me`、身份头透传 `X-User-Id=1001|Role=CONSUMER|Mfa=true|Jti=jti-1001`、Authorization 保留** / **白名单路径伪造身份头被剥离(null)** / 内部接口 404 / 路径混淆 `%2e` 404 / 未知服务 404 / 慢下游 2.5s→503+5002 / 降级端点外部直访 404 / **限流 3×200 → 2×429+2004** / 演练后恢复 200;
  4. **任务 12.2 故障注入(真机实证)**:双实例(18081 instanceId=local / 18082 gateway-b)同时服务 → 停止 A 后 **A 不可达、B 继续服务(health UP + routes 200)**;"全实例不可用 → 入口 503"属 LB/Nginx 层行为(配置样例已给,不做直连应用降级),真机需 Nginx/K8s 复演;
  5. **任务 11.1 等价运行验证**:沙箱无 docker → 用「网关 fat jar 双实例 + Node 桩」完成等价的运行验证(见 12.1/12.2);Dockerfile/compose/Nginx 样例文件已交付并通过 YAML 校验。
- **验证**:`ACC mvn verify` **251 用例 0 失败**;`mvn -f services/gateway/pom.xml verify` **132 用例 0 失败、jacoco 行 94.1%/分支 84.6%**;演练 13 项 + 故障注入输出留痕;`openspec validate implement-gateway-service` 通过;任务勾选 **29 完成 / 2 部分 / 1 未完成(9.2 待实际提交时执行)**。
- **关键决策与理由**:① ACC 侧 401 语义由"验签失败"改为"**缺身份头**(fail-closed)",内网直连无身份即拒,与"网关唯一鉴权点"一致;② 测试迁移用 `RequestPostProcessor` 而非改测试框架,单点注入、改动面可控;③ 吊销写入方**只固化契约不造接口**(接口变更需产品决策 + openapi 权威源同步);④ 演练用 Node 桩替代真实 ACC(沙箱 ACC 为占位数据源、DB 调用明确失败),以"下游回显"证明重写与透传。
- **经验与教训**:① 演练要先**预热下游**(Node 冷启动首请求 >1s 会撞上路由 1s 超时 → 误判为网关缺陷,并会把熔断打开导致后续全部 503/5002);② 本环境 `System.Net.HttpListener` **不可用**(PlatformNotSupported)→ 桩用 Node;③ 沙箱**禁用 CIM 查进程**(拒绝访问)→ 停实例改用 job 管理;④ 环境里 `pwsh` 不存在(实为 Windows PowerShell),脚本调用需用 `powershell`。
- **遗留(环境/决策受限)**:(a) 10.2 真机双进程联调(需 ACC 可运行数据库;两侧测试证据已具备);(b) 11.1 `docker compose up` 运行验证(需 Docker);(c) 12.2「全实例不可用 → Nginx 503」真机复演(需 Nginx/K8s);(d) Gate C:I9 的 `sub` 必填与 `exp` 上界(需策略确认)、`rate-limit.lua` 独立测试;(e) 9.2 `commit-check` 待实际提交执行(工作区另有既有改动,提交需拆分粒度)。

## 会话 7:真机双进程联调(10.2)+ 令牌策略定档与 I9 加固 + ACC 数据层缺陷修复

- **主题**:按用户"继续按顺序进行"推进 ①真机双进程联调 ②I9 令牌策略 ③提交准备;用户裁决:ACC 缺陷走「最小修复 + 登记已知限制」,I9 采用**双 token**(短 token 15 分钟、长 token 7 天存 Cookie)
- **需求/问题**:沙箱无 Docker、外部 MySQL 服务无法启动(需管理员),但 `services/acc/pom.xml` 已有 test 作用域 `mariaDB4j`(嵌入式 MariaDB)——据此把「真机联调」从"环境不具备"变成"可执行",并因此抓到装配层真实缺陷
- **方案/动作与产出**:
  1. **真机联调桩(10.2 交付物)**:新增 `services/acc/deploy/drill/{AccDrill.java, build-drill-classpath.ps1, README.md}`——嵌入式 MariaDB(mariaDB4j,test 作用域)+ Flyway V1/V2 + `@Primary` 真实 DataSource 覆盖 M1 占位数据源 + 真实 `AccApplication` 进程;夹具账户 `account_id=1001`;`token` 模式用 ACC 自身 `JwtCodec` 签发演练 token(与未来登录签发同路径);**非生产代码、不在 Maven 源根下、不参与构建**;
  2. **真机链路实测**:`curl → 网关(鉴权/限流/熔断/改写) → ACC(真实进程) → MyBatis → MariaDB`。网关侧:15 分钟 token **200** / 1 小时 token **401+2001** / 无 `sub` token **401+2001** / 伪造 `X-User-Id:9999` + 合法 token 仍返回 **`acc_1001`**(伪造头被剥离) / 白名单 `/api/v1/acc/captcha` **200** / 内部接口 **404**(**直连 ACC 同路径 200**,证明拦截发生在网关) / 路径混淆 **7 变体全部 404**;ACC 直连:`/acc/captcha` 200、无身份头 `/acc/me` **401**(fail-closed)、带身份头 **200**(`138****8000`/`张*丰`,脱敏来自真实解密)、库内 `mobile` 为 **AES-GCM 密文**(外部 mysql 客户端核验);
  3. **真机联调发现 ACC 数据层缺陷并修复**:`AccConfiguration` 6 个 Mapper Bean 原为 `factory.openSession().getMapper(...)`(会话长驻、`autoCommit=false`、事务永不提交),真实库下三症状——①**读陈旧**(外部已提交 `wallet_status='FROZEN'`,接口仍返回 `ACTIVE`)②**写不落库**(`POST /acc/account/close` 返回 200 但 `closed_at` 仍为 NULL)③**持锁阻塞外部写入**(外部 UPDATE 撞未提交事务行锁,`Lock wait timeout exceeded`,约 50s)。修复:`factory.openSession(true)`(自动提交,与 `repository` 层 DAO 测试同口径);修复后实测外部提交后接口读到 `FROZEN`、`close` 后外部 `SELECT` 得到 `closed_at=2026-09-15 19:52:29.212, close_reason=drill-fixed`;
  4. **回归防线**:新增 `services/acc/src/test/java/com/msz/acc/config/RealDbAssemblyTest.java`(真实库 + 真实 Tomcat + 真实 HTTP,2 用例,分别拦「读陈旧」与「写不落库」;用 `@ConditionalOnProperty(acc.test.realdb)` 隔离,避免被既有 `@SpringBootTest` 的组件扫描误装);把库启动逻辑抽到 `testsupport.EmbeddedMariaDb` 供 DAO 测试与装配测试共用;
  5. **依赖钉版**:`services/acc/pom.xml` 显式钉 `jakarta.annotation:jakarta.annotation-api:2.1.1`——test 作用域 mariaDB4j 传递 javax 时代的 `1.3.5` 抢占依赖调解,令 Web 容器启动抛 `NoClassDefFoundError: jakarta/annotation/PostConstruct`(真机联调发现);
  6. **令牌策略定档 + I9 加固(网关侧)**:`gateway.auth.access-token-max-ttl`(默认 `15m`,env `GATEWAY_ACCESS_TOKEN_MAX_TTL`)、`gateway.auth.clock-skew`(默认 `60s`,env `GATEWAY_CLOCK_SKEW`);`JwtVerifier` 增:①`sub` 必须存在且非空白 ②`exp` 必须存在、未过期且**不超过 max-ttl + clock-skew** ③`alg` 必须 HS256;**过期判定严格**(容差只放宽上限);长 token(refresh,7 天)存 HttpOnly+Secure+SameSite Cookie、不经网关业务链路,仅用于换发短 token;**换发接口尚未实现**(M1 无登录接口,属接口清单新增,登记不改);
  7. **次级文档同步**:`services/gateway/docs/README.md`(§3.2 令牌策略 + §6 配置键/实现要点/质量基线)、`services/gateway/deploy/README.md`(env/配置行/验证清单第 14 条/已知约束)、`services/acc/docs/README.md`(§6 已知限制与实现要点)、本日志 + `PROJECT_LOG/2026-09-15.md`。
- **验证**:网关 `test` = **139 用例 0 失败**(原 132,+6 令牌策略用例 +1 配置绑定用例);ACC `mvn verify` = **253 用例 0 失败**(原 251);**反向验证**:把 `openSession(true)` 临时改回原实现,`RealDbAssemblyTest` 立刻 **1 Failure**(`closed_at` 为 NULL)+ **1 Error**(`Lock wait timeout exceeded`),恢复后复绿 → 回归测试有效性已证;真机演练全链路与路径混淆 7 变体留痕;ACC 库里 `mobile` 密文与解密后脱敏输出双侧核验。
- **关键决策与理由**:① **双 token 时效定档**(短 15min / 长 7day Cookie)——原 PDD 中「JWT 时效」为【待评审】阈值项,本次由用户裁决落地;② ACC 缺陷取**最小修复 + 登记残余限制**(长驻会话、跨表无原子性留 M2「按请求会话 + 显式事务边界」收敛),不顺手重构数据层,避免交付期扩大改动面;③ 换发接口**只固化策略不臆造接口**(接口变更需 openapi 权威源 + 前端 api-client 同步);④ 真机联调用**嵌入式真实库**替代外部 MySQL/Docker,让"环境不具备"的验收项变成可复跑的桩 + 清单。
- **经验与教训**:① **`@ConfigurationProperties` 记录绑定只允许唯一构造器**——给嵌套记录再加一个构造器会让 Spring 找不到绑定入口,实测 `gateway.auth` 绑定为 `null`(12 个上下文用例同时变红);需要默认值时用**静态工厂**(`Auth.of(...)`),不要加第二构造器;② **test 作用域的依赖也会经依赖调解污染运行期 API**(mariaDB4j → javax 时代的 `jakarta.annotation-api:1.3.5`),表现为容器启动期 `NoClassDefFoundError`,必须显式钉版;③ **未提交的长事务不只是数据不正确**:它持行锁把外部写入卡到 `innodb_lock_wait_timeout`(约 50s),这也是反向验证那次耗时 65s 的原因;④ **mock 覆盖不到的装配层会话语义只能靠真机验证**——`PROJECT_LOG/2026-09-14.md` 早已把 `openSession().getMapper()` 记为"生产装配前必修",本次由真机把"风险"确认为"缺陷"并补上回归测试。
- **下一步(未完成,按序)**:
  1. 9.2 `commit-check` 门禁 + 按粒度拆分提交(工作区另有非本次变更的既有改动);
  2. 环境受限项复演:11.1 `docker compose up`(需 Docker)、12.2「全实例不可用 → Nginx 503」(需 Nginx/K8s);
  3. 待接口清单流程:refresh 换发接口(含 Cookie 属性与 CSRF 口径)、吊销写入方(登出/踢下线);
  4. Gate C 余项:`rate-limit.lua` 独立测试。

## 会话 8:ACC 请求级事务边界落地(`fix-acc-transaction-boundary` 全流程:基础设施 → 装配切换 → 真实库用例 → 门禁与真机复跑 → 文档同步)

- **主题**:把 2026-09-15 真机联调止血(`factory.openSession(true)`)留下的三项残留——**会话长驻、跨表写入无原子性、无事务边界**——按新变更 `openspec/changes/fix-acc-transaction-boundary/` 收敛为「**一个请求 = 一个事务**」;本次会话完成该变更任务组 1/2/3(另一位 implementer)之后的**组 4(门禁与真机复跑)+ 组 5(文档同步)**,并顺手修掉任务评审的两个 Minor
- **需求/问题**:①ACC 的 6 个 Mapper Bean 在装配期各开一个会话并永久持有(`autoCommit=false`,事务永不提交),止血改成自动提交后虽「写即落库、读即最新」,但**同一请求内的跨表写入仍无原子性**(失败会留半成品)、**无事务边界**、且并发请求共享同一 `SqlSession`/`Connection`;②既有测试覆盖不到装配层会话语义(6 个控制器测试 `@MockBean` 替换全部 Mapper 不触库,DAO 测试用自动提交会话),需要**真实库 + 真实 HTTP + 外部连接视角**的用例兜底;③仓库侧口径(`services/acc/docs/README.md` §6、`docs/待评审事项汇总.md` E-10、`implement-gateway-service` design D16 / tasks §14.3)仍写「M2 收敛」,与代码已落地的事实矛盾
- **方案/动作与产出**(design D1~D10,提交分三批):
  1. **组 1 基础设施(零行为变化)**:新增 `repository/RequestSqlSessionHolder`(`ThreadLocal<SqlSession>` 绑定请求级会话;**首次真正访问数据库时才惰性** `openSession(false)`;请求结束 `close()` + `remove()`;无请求上下文降级为「单次自动提交会话 + 调用后关闭」)、`infrastructure/tx/TransactionalMapperProxy`(`java.lang.reflect.Proxy`,每次调用转发到当前会话的 Mapper,`InvocationTargetException` 解包后原样抛出)、`infrastructure/tx/TransactionBoundaryFilter`(`FilterRegistrationBean`,order 2、`urlPatterns=/acc/*`)——提交 `c3ed31f`;
  2. **组 2 装配切换**:`AccConfiguration.mapper(...)` 由 `factory.openSession(true).getMapper(type)` 改为事务代理,6 个 Mapper Bean 的名称/返回类型/全部构造注入点不变;`mapper(...)` 注释改写为请求级事务语义并**保留 2026-09-15 真机联调缺陷历史**;实现期补漏(控制器裁定 R-6):Spring MVC 的 `@RestControllerAdvice` 会把异常就地转成错误 Envelope,异常**不会抵达过滤器**,故 `GlobalExceptionHandler` 写出错误 Envelope 时置位请求属性 `acc.transaction.rollbackOnly`,事务边界见标记即回滚(否决「按 `response.getStatus() >= 400` 回滚」的状态码启发式)——提交 `4238f8b`;
  3. **组 3 真实库装配测试扩展**:复用 `config/RealDbAssemblyTest` + `testsupport/EmbeddedMariaDb`,以「真实库 + 真实 Tomcat + 真实 HTTP + 外部连接视角」新增跨表回滚(复用真实 `uk_open_id` 约束注入失败,反向验证先红后绿)、并发隔离(N≥8 混合读写)、提交可见性、幂等槽位释放(失败后同 key 重试真正执行)、读路径契约(3006 等业务码不受事务机制影响)、连接释放、并发同 key 竞态——提交 `35a567b`;
  4. **组 4 门禁与真机复跑**:`mvn -f services/acc/pom.xml verify -nsu` = **280 用例 0 失败 BUILD SUCCESS**(组 4 门禁当次输出为 279,其后并入 tasks 3.8 守卫用例,终态 280);按 `services/acc/deploy/drill/README.md` 复跑**双进程 + 真实数据库**联调(网关 fat jar 18081 + 真实 ACC 8080 + 嵌入式 MariaDB 33061),矩阵与 2026-09-15 记录一致并追加写路径复验(见验证);
  5. **组 5 文档同步(5.1~5.5)**:`services/acc/docs/README.md` §6 把「会话仍为长驻(非按请求)…跨表写入无原子性…M2 收敛方向」改写为**已落地请求级事务边界 + session-per-request**(保留 2026-09-15 缺陷历史与 253 用例历史数值);`docs/待评审事项汇总.md` E-10 改「**已由 `fix-acc-transaction-boundary` 承接并落地**」;`openspec/changes/implement-gateway-service/design.md` D16 残留限制段与 `tasks.md` §14.3 追加承接注记(**任务状态标记不变**,与该变更交叉引用口径一致);`docs/design/高并发架构演进设计.md` §2.5 连接池口径补「**ACC 请求级事务使连接持有时间等于请求时长**,压测标定 TBD-6 时计入」(**既有连接池初值 `20~50` 不变**);本日志 + `PROJECT_LOG/2026-09-15.md`;
  6. **评审 Minor 修复(F1/F2)+ 归档清理(O-3)**:F1——`TransactionBoundaryFilter` 的受检异常口径注释改为与**已生效的 design D6 实施期修订(R-7)**一致(「未捕获异常含受检一律回滚」,删除「与 design D6 的差异 / 受检异常提交」旧表述),同步修 `TransactionBoundaryFilterTest` 类 javadoc 与用例 `@DisplayName`;F2——把「6 个 `@MockBean` 控制器测试 = D4 惰性开启判据」这一**不成立**的表述(MyBatis `openSession()` 本身不取连接,急切开会话不会让那些用例变红)在 `RequestSqlSessionHolder` javadoc 中更正为**真实判据 = 单测对 `SqlSessionFactory` 的 mock 断言「未触库时 `openSession` 零调用」**;O-3——删除中间态日志 `services/acc/target/realdb-green.log`(补失败信号前 `Failures: 3` 的日志,文件名误导)
- **验证**:`mvn -f services/acc/pom.xml verify -nsu` → **Tests run: 279, Failures: 0, Errors: 0, Skipped: 0 / BUILD SUCCESS**(01:17;注释修正前后各跑一次,结果一致);真机双进程矩阵(curl 直出响应体留痕):`/gateway/health` **200 UP**(`instanceId=gateway-drill`)、`/api/v1/acc/me` 无 token **401+2001**、15 分钟短 token **200**(`acc_1001`/`138****8000`/`张*丰`,路径已改写 `/acc/me`)、1 小时超长 token **401+2001**、无 `sub` token **401+2001**、白名单 `/api/v1/acc/captcha` **200**(真实 `cap_93330377979990016`)、网关内部接口**带 token 404+3006**(**直连 ACC 同路径带 `X-Internal-Token` 200**,证明拦截在网关;无 token 时先被鉴权拦成 401)、直连 ACC 四身份头 **200**、直连 ACC 无身份头 **401**(fail-closed);**写路径外部视角复验**:`POST /acc/account/close` → **200 + closedAt**,外部 `mysql` 连接立即读到 `closed_at=2026-09-15 21:02:16.391, close_reason=g4-drill-recheck`(2026-09-15 F-2「写不落库」不复现),外部提交 `UPDATE wallet_status='FROZEN'` 后 `/acc/me` 立即返回 **FROZEN**(「读陈旧」不复现)。**结论:事务改造未引入回归**。收尾已 kill 两个进程并确认 8080/18081/33061 全部释放
- **关键决策与理由**:①**不引 `spring-boot-starter-jdbc` / `mybatis-spring`**(D1)——ACC 既有风格是「不引 starter、显式装配」,引框架会把「谁持有连接」藏进框架并引入第二套会话语义;②**会话绑定用 `ThreadLocal`**(D2)而非 `RequestContextHolder`(`SqlSession`/`Connection` 非线程安全);③**Mapper 保持单例、内部改动态代理**(D3)——20+ 处构造注入点与全部既有测试不动;④**惰性开启是硬约束**(D4)——不触库的请求(如 `/acc/captcha`)不占连接;⑤**显式失败信号优先于状态码启发式**(R-6)——`rollbackOnly` 请求属性语义清晰且可测;⑥**未捕获异常含受检一律回滚**(R-7)——spec 是约束权威,「受检异常默默提交半成品」是更危险的失败模式(有意偏离 Spring 默认);⑦文档同步**保留历史、只改现行口径**——历史缺陷记录与既有数值不得改写
- **经验与教训**:①**mock 覆盖不到的装配层会话语义只能靠真机 + 真实库验证**——本次三项残留全部由「真实库 + 真实 HTTP + 外部连接视角」用例锁死,且失败注入用真实约束(`uk_open_id`)而非测试钩子,先红后绿可反向验证;②**`@RestControllerAdvice` 会把异常「就地消化」**——事务边界只看 `catch` 分支会把失败请求当成功提交,必须由错误 Envelope 的产出方显式告知边界(R-6);③**判据要有判别力**:MyBatis `openSession()` 只赋字段不取连接(`getConnection()` 才建连),故「6 个 `@MockBean` 控制器测试全绿」**不能**证明惰性开启——真正的判据是单测里「未触库时 `openSession` 零调用」的 mock 断言(评审 F2);④本机 `powershell`(**Windows PowerShell 5.1**)读**无 BOM 的 UTF-8 脚本**会按 ANSI 解码,`services/acc/deploy/drill/build-drill-classpath.ps1` 因此报「Missing closing '}'」类解析错误——本次以 **BOM 副本**运行完成演练(见遗留项)
- **下一步(未完成,按序)**:
  1. 本变更工件(`openspec/changes/fix-acc-transaction-boundary/{proposal,design,spec,tasks}.md`)**尚未纳入 git**(工作树为 untracked),归档/提交时机待控制器决定;
  2. `openspec/changes/fix-acc-transaction-boundary/design.md` D4 括号内「若入口急切开启会话,这 6 个类的用例会集体变红」与 `tasks.md` 2.1 的判据表述仍是评审 F2 指出的**不成立推断**(本次已更正代码侧注释,该两处 MD 属变更工件、按范围未改),建议随工件提交一并更正;
  3. `implement-gateway-service` tasks §6.1/6.2 登记的仓库级待决项(唯一键冲突错误码 3008、写库服务统一 jacoco 门禁)仍开放;
  4. `services/acc/deploy/drill/build-drill-classpath.ps1` 建议补 UTF-8 BOM(或在 README 注明改用 PowerShell 7),免后续 Windows PowerShell 5.1 环境解析失败。

**补记(2026-09-15,复评 N4;仅追加,不改写既有条目)**:上述「下一步」第 2 项——变更工件 `design.md` D4 与 `tasks.md` 2.1 的判据表述**已由控制器更正**;第 4 项——演练脚本 BOM **已随 `f63c6a3` 提交**,并已在根 `.editorconfig` 显式登记 `[*.ps1]` 的 `charset = utf-8-bom` 例外;本变更**终态用例数为 280**(上文「验证」段保留的 `Tests run: 279` 系该次运行的原始命令输出,属历史留痕,不改写)。

## 会话 9:superpowers/SDD 执行 —— 任务三(ACC 事务边界)落地 + 任务一(双 token 会话)主体实现

**会话信息**:主题 = 按 superpowers 整套流程实施两个 OpenSpec 变更 · 目标(用户原话)=「按照 superpowers 的整套流程,完成任务一和任务三;1. 研读规范、拆解任务 2. 完成代码自查与评审」

**过程记录**

1. **需求/问题**:先给方案再实施(工作区规则一),并明确任务一=双 token 轮换(refresh 换发)、任务三=ACC 事务边界收敛。
   **方案/动作**:拆成两个 OpenSpec 变更(接口变更 vs 内部重构,可独立评审/归档);用 `openspec-propose` 生成 4+4 工件;就 4 个影响范围/对外行为的决策点给选项(登录入口是否含首次签发 / 签发端点归属 / 轮换严格度 / 事务实现路径),用户**全选推荐**。
   **产出物**:`openspec/changes/add-refresh-token-rotation/`(proposal + specs/acc-session & platform-gateway + design D1~D8 + tasks 30 条)、`openspec/changes/fix-acc-transaction-boundary/`(proposal + spec 6 需求 + design D1~D10 + tasks 22 条)。
   **验证**:`openspec validate --strict` 两变更均通过;spec 语言与仓库既有 `platform-gateway` spec 对齐(英文)。

2. **需求/问题**:实施(先任务三,再任务一)。
   **方案/动作**:用 subagent-driven-development —— 每变更先建 ledger(`.superpowers/sdd/<plan>/progress.md`)+ pre-flight 冲突表 + 执行前裁定;把计划书面顺序改为 **基础设施 → 用例(先红) → 装配切换(转绿)**(原顺序下 3.1「用例在修复前必须失败」不可观测);每单元一名 implementer(禁派子代理、禁越界)、任务评审子代理(spec 合规 + 质量双结论)、Minor 登记不入修复循环。
   **产出物**:任务三 6 提交 `290d66e..f63c6a3` —— `RequestSqlSessionHolder` / `TransactionalMapperProxy`(Mapper 事务感知代理)/ `TransactionBoundaryFilter`(惰性开启 + 提交/回滚 + `ThreadLocal.remove()`)+ `GlobalExceptionHandler` 显式回滚标记 + `RealDbAssemblyTest` 10 例(含跨表回滚、并发隔离、幂等槽位释放、提交可见、连接释放、并发同 key 竞态、未提交写不可见)。任务一 5 提交 `5c92aff..da2f98f` —— `SessionStore` 端口(InMemory + `RedisSessionStore`/`AccLettuceStringRedisOps`)/ `SessionFlow`(登录签发、单次使用轮换、会话族重用检测、并发宽限、登出吊销)/ `AuthController` + `SessionCookie` + `OriginValidator` + `AuthPathMatcher` + 网关白名单与 Cookie 透传增量(44 文件 +4937/−23)。
   **验证**:任务三 ACC **280 用例 0 失败** BUILD SUCCESS(基线 253);任务评审 spec 6 需求/11 场景**全 ✅**、质量 0 Critical/0 Important/6 Minor;增量复评 F1/F2/F3/O-3 全 **ADDRESSED**、新增 1 Important + 7 Minor;任务一 ACC **381 用例**、网关 **150 用例**(含 jacoco 门禁)**全部 0 失败**;真机双进程复跑矩阵与 2026-09-15 记录逐项一致并加验「写后读」(无回归)。

**关键决策与理由**

- **拆两个变更**:任务一是对外接口变更(需 openapi 权威源 + 前端约定),任务三是内部数据层重构;**R-A1 串行执行**(两者都改 `AccConfiguration` 与 ACC 测试,且任务一的写入路径应落在任务三的事务边界内)。
- **签发端点全部落 ACC、网关只做校验**(D1):网关无业务库,放进去需回源 ACC,破坏「网关无业务数据」边界;网关只加白名单(login/refresh,不含 logout)与 Cookie 透传。
- **refresh 单次使用 + 轮换 + 会话族重用检测**(D2):7 天凭据是最大攻击面;重用检测把泄露影响收敛为「整族即时失效」。
- **错误码复用 2001,不新增**(D5):客户端处置一致(重新登录),区分只会泄露「该 token 是否存在/被重放」;可观测性由审计日志承担。
- **Redis 形态**:ACC 引入 `io.lettuce:lettuce-core` 直接依赖 + 手写 ops(与网关同风格),否决 spring-data-redis 全家桶;真机跨进程验证用 `embedded-redis`(jar 内含 Windows 原生 redis-server,沙箱实测可用,无需 Docker)。
- **计划缺陷由实证回改**(而非迁就实现):① MyBatis `openSession()` **不取连接**,故「入口急切开启会让 6 个 `@MockBean` 测试变红」的论证不成立(design D4 已改写为 spec 依据 + 单测判据);② `@RestControllerAdvice` 会**就地消费异常**,异常不抵达 Servlet 过滤器 → 失败请求会被当成功提交,必须由 advice 显式打回滚标记(design D6 补漏段);③ 受检异常口径以 **spec 为权威**改为「未捕获异常一律回滚」。

**经验与教训**

- **计划顺序会杀死 TDD**:把「修复」写在「用例」之前,RED 就不可观测;执行前必须按「先红后绿」重排。
- **裁定依据本身也会错**:ledger 里的 Ruling 让子代理无需打断用户即可推进(本次 12+ 条),但 D4 那条论证被评审用 `javap` 实证推翻——评审必须有权质疑计划文本。
- **环境坑**(可复刻):编辑工具会**吃掉 `.ps1` 的 BOM**(Windows PowerShell 5.1 解析非 ASCII 脚本必需,须重补并加 `.editorconfig` 例外);`jar xf` 会把整个 jar 解到仓库根(须在临时目录解包);**行尾混行会让 diff 整文件化**(插入文本要与目标文件行尾一致,改完核对 `git diff --stat`)。
- **子代理任务书的边界条款有效**:「禁改区 + 发现缺口回报」避免了越界与自造接口(如未擅自扩展 `_common` 的 `StringRedisOps`)。

**下一步**

1. 修复波:复评的 1 Important(N1 网关工件 §14.3 状态自相矛盾)+ 7 Minor,清单已备 `.superpowers/sdd/fix-acc-transaction-boundary/fix-wave-n.md`(N4 已裁定 parked、N5 已由控制器修)。
2. 任务一收尾:组 6/7/8 —— 真机演练内嵌 `embedded-redis` 跑通「建族 → 轮换 → 重放被拒且整族吊销 → 登出后短 token 立即 401」;**L1 风险待处理**(`acc.redis.host` 为空时 ACC 静默退化为内存会话存储 → 登出/踢人失效,需部署必填或 profile fail-fast);任务 1.6 承接标注(由控制器做,`openspec/**` 属子代理禁改区)。
3. 最终整体评审(一次覆盖两变更)→ `commit-check` → 按粒度提交(两个变更工件目录仍未入库)。

## 会话 10:复评修复波执行(N1 Important + N2/N3/N6/N7/N8 + 任务一 1.6 承接标注)

**会话信息**:主题 = 执行 `.superpowers/sdd/fix-acc-transaction-boundary/fix-wave-n.md` 的修复清单(1 Important + 6 Minor,另并入任务一 1.6);执行者 = 控制器指派的**一名** implementer(禁派子代理)。

**过程记录**

1. **需求/问题**:复评 `4238f8b..f63c6a3` 给出 N1~N8(N5 已由控制器修、N4 裁定 parked:日志只追加不改写);清单要求顺带完成变更一 `add-refresh-token-rotation` 任务 1.6(承接标注,原定控制器做,因同文件冲突并入本波)。
   **方案/动作**:先分清边界——**N1 修 D16/§14.3 的状态自相矛盾(改为「交付内容已承接落地;该行状态保持不变」),1.6 加 D15/§14.1 的承接注记(只追加,`- [ ]` 不动)**;四处一律「只追加承接说明,不宣告任何任务状态变更」。
   **产出物**:`openspec/changes/implement-gateway-service/{design.md D15/D16,tasks.md §14.1/§14.3}`、`openspec/changes/add-refresh-token-rotation/tasks.md §1.6`。
2. **需求/问题**:N8 —— tasks 3.8 用例的收尾恢复不在 `finally`,提交后断言失败会残留 `FROZEN`(重复运行先在前置断言处失败,掩盖真实原因)。
   **方案/动作**:`restoreWalletStatusActive(77004)` 移入 `finally`(回滚在途写 → 关连接 → 恢复 ACTIVE),注释写明「收尾恢复必须无条件执行,保证用例可重复运行」;断言链与生产代码零改动。
   **验证**:`mvn -f services/acc/pom.xml test -Dtest=RealDbAssemblyTest -Djacoco.skip=true -nsu` → **Tests run: 10, Failures: 0, Errors: 0, Skipped: 0 / BUILD SUCCESS**。
3. **需求/问题**:N7 —— 演练脚本必须带 BOM(Windows PowerShell 5.1 解析非 ASCII 脚本),与根 `.editorconfig` 的 `charset = utf-8` 字面冲突。
   **方案/动作**:根 `.editorconfig` **追加** `[*.ps1]` + `charset = utf-8-bom` 例外段(附一行中文原因注释);脚本 BOM **保留**。
4. **需求/问题**:N2/N3/N6 —— E-10「无残留限制」过头、终态用例数四处仍是 279、高并发文档缺变更行。
   **方案/动作**:E-10 改「三项原残留已收敛 + 新登记残留见 `fix-acc-transaction-boundary/design.md` D6 已知残留段」;四处 279→**280**(ACC README / 本日志 / PROJECT_LOG / gateway tasks §14.3)并在 ACC README「回归防线」补 tasks 3.8 守卫用例(标注「守卫用例,未观测到 RED」);高并发 §2.5 口径**并入既有** `变更 v0.9（2026-09-15）` 行(不新增同版本行)。
   **验证**:全仓 `279` 命中仅剩历史留痕(AI_DEV_LOG「验证」段当次命令输出 `Tests run: 279`,按「只追加不改写」保留);N4 以**追加补记**形式在两份当日日志落地。

**本波提交**

- `12ac50e` `docs: 复评修复波 N1/N2/N3/N6/N7 与任务一 1.6 承接标注`(8 文件 +83/−9;含两份日志中此前未提交的「会话 9」记录 +38/+28 行)
- `c9a8c40` `test(acc): 3.8 守卫用例收尾恢复移入 finally（保证可重复运行）`(1 文件 +6/−2)
- 回填提交:任务一 `tasks.md` §1.6 标 `[x]` 并写明由本波执行 + 哈希 `12ac50e`(即包含本段记录的提交)

**关键决策与理由**

- **承接注记 ≠ 状态变更**:D15/§14.1/D16/§14.3 四处均只追加承接说明,与 `add-refresh-token-rotation` tasks §1.6「原变更任务清单状态不变(除标注)」同口径——不把未完成项宣告为已闭环。
- **历史数值只保不改**:「原 253」「原 251」与当次原始输出 `Tests run: 279` 逐字保留,只同步「当前/终态」口径(280);N4 的更正以追加补记落地。
- **N6 不新增同版本行**:变更一已追加 `变更 v0.9`,故 ACC 连接持有口径并入同一行,避免出现两行同版本号。

**经验与教训**

- **PowerShell 调 Maven 要给 `-D` 加引号**:`-Djacoco.skip=true` 被拆成 `-Djacoco` + `.skip=true` → `Unknown lifecycle phase ".skip=true"`;写成 `"-Djacoco.skip=true"` 正常。
- **中文标点风格逐文件核对**:本仓库多数 MD 用半角括号/逗号,ACC README 与设计文档用全角;插入前先看目标段落风格,否则同段落内标点不一致。

**下一步**

1. 控制器:`commit-check` 门禁(本机 `.githooks` 的 `sh.exe` 崩溃,钩子不执行)与两变更工件首次入库;
2. 任务一收尾:组 6/7/8(真机 `embedded-redis` 会话闭环、`acc.redis.host` 为空的 L1 fail-fast);
3. 本波未做:`AI_DEV_LOG/README.md`、`PROJECT_LOG/README.md` 的会话索引/看板同步(不在本波可改文件清单内)。
