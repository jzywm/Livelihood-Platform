# 项目日志 2026-09-16

> 承接 2026-09-15(两变更实现与真机联调);本日为评审整改与交付收口。

## 今日目标(【测试】【编码】【发布】)

- 【测试】对两个 OpenSpec 变更做**最终整体评审**,并对 Critical/Important 完成整改与定向复评
- 【编码】修复评审发现的真实缺陷(含族寿命塌缩 Critical、来源校验口径、网关保留原始 Host)
- 【发布】执行提交门禁并把两变更工件与代码按粒度入库

## 完成

- 【测试】**最终整体评审**(区间 `290d66e..5a08850`):16 需求 / 34 场景 → **33 ✅ / 1 ❌**;1 Critical + 2 Important + 若干 Minor;产出 `.superpowers/sdd/final-review.md`
- 【编码】**修复波 FIX-1**(`b4710e0` 代码/测试、`2782580` 文档/部署件):A1 族寿命塌缩(Critical)、A2 来源校验同源默认放行、C1 会话存储两方向对称校验、B1~B11 顺修;验证:ACC **410 用例 0 失败(1 跳过)**、网关 150 全绿 + jacoco 通过、真机 **34 项断言 0 失败**(族键 TTL 实测 604800,修复前 864~900)、C1 反向验证留档
- 【测试】**定向复评**:A1/A2/C1 + B1~B10 全 ADDRESSED、B11 PARTIALLY;新增 1 项 Important(族记录 status 语义竞态,登记 §10.1);结论可合并
- 【编码】**网关保留原始 Host 收口**(`a9e7188`、`e6feaa0`):acc 路由启用 SCG `PreserveHostHeader`(不改 Java)+ 集成测试 RED→GREEN + 部署口径与演练用例(34→37 断言);验证:网关 **154 用例 0 失败 + jacoco 通过**;真机 drill3 **37/37**(浏览器默认形态换发 200 且真轮换;跨源 401 且不轮换)
- 【发布】**门禁与提交**:`commit-check` 语义清单 A~F + 物理 hook 规则等价扫描(0 冲突/0 私钥/0 空白/0 大文件;凭据粗筛 7 处逐条定性,1 处 fixture 已修);两变更工件入库 + `openapi.apifox.json` 重打包(接口计数 19→18);提交 `d89a8d1`、`573a3be`

## 决策与发现

- **真机演练是唯一能暴露这类缺陷的关口**:两处真实生产缺陷(L4 真实 Redis 上 Lua 整数回复解码错误 ⇒ 登录 503;A1 族标记寿命塌缩 ⇒ 超窗重放不再整族吊销)都只在真实依赖 + 跨进程演练中暴露,假客户端单测全绿放行
- **同源校验依赖入口保留原始 Host**:SCG 默认改写 `Host` ⇒ 浏览器→Nginx→网关→ACC 拓扑下 ACC 推断不出对外来源;裁定「网关侧保留原始 Host 为主、Nginx `X-Forwarded-Host` 为备案」,并补浏览器默认形态真机用例
- **Minor 不入修复循环**:登记台账交最终评审分诊;Important/Critical 才进修复波(避免无限循环)
- **被中断的并行小修复成果并入修复波**:避免同文件丢更新,并在提交正文标注来源

## 问题与阻塞

- **§10.1 [Important] 族记录 `status` 语义竞态**(已登记,docs/openspec tasks §10):登出若落在 `SessionFlow.refresh` 的 consume 与 rotate 之间,轮换会把族写回 ACTIVE;修法需改 `SessionStore` 契约(端口返回「已吊销族拒绝轮换」信号),按评审结论另立一波
- **§10.2 [Minor] HTTPS 入口须透传 `X-Forwarded-Proto`**:已写入部署手册/nginx 样例/ACC 基线,待部署环境复核
- **环境受限**:`docker compose up`/镜像构建(沙箱无 Docker)、池化 DataSource 与网络 Redis 的池水位标定(TBD-6)、浏览器/前端侧验证(`apps/**` 仍为脚手架)、ACC 无 jacoco 覆盖率门禁(仓库级决策)
- **物理提交 hook 不可用**:本机 `sh.exe` 崩溃(`couldn't create signal pipe, Win32 error 5`),已按 hook 规则等价扫描复核;CI G2 仍为兜底

## 明日计划

1. 处置 **§10.1**(族记录状态语义竞态):设计端口层「已吊销族拒绝轮换」信号 + consume/rotate 交错用例,单独一波修复
2. 有 Docker/数据库的环境复跑 `docker compose up`、池化 DataSource 与 Nginx 503 复演,并按 TBD-6 标定连接水位
3. 本机推送分支(`git push -u origin feature/accountsfunds开发`)并复跑仓库 hook 门禁;`feature/accountsfunds开发` → main 的合并策略待定
4. 视需要把两个变更 `openspec archive`(归档后 `openspec/specs/` 将首次出现 `acc-session`/`acc-transactional-data-access`/`platform-gateway` 基线)
