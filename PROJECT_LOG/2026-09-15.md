# 项目日志 2026-09-15

## 今日目标
【编码】按「M1 初期交付即接管」口径交付网关服务 GATEWAY(实现 → 评审修复 → 门禁 → 部署件 → 验收演练),并同步完成 ACC 瘦身(网关唯一鉴权点)、真机双进程联调(10.2)与令牌策略定档。

## 完成

- 【编码】GATEWAY 服务交付:S1 骨架 / 错误码与统一 Envelope / 统一鉴权链 / 网关级限流 / 路由与超时 / 熔断降级 / 运维接口全部落地(`services/gateway/`);代码评审 Gate A/B(3 Critical + 16 Important)全修——入口净化剥离伪造 `X-User-*`、路径混淆拒绝、熔断实例按路由命名、可执行 fat jar 打包、Redis AUTH、运维接口网段白名单、限流 key 路径变量归一 `{id}`、XFF 信任开关、traceId 格式白名单等。验证:`mvn -f services/gateway/pom.xml verify` 全绿 + jacoco 行 94.1% / 分支 84.6%(门禁 80/75)。
- 【测试】任务组 12 验收演练(真机):网关 fat jar(18081)+ Node 下游桩(8080)——13 项验证清单全绿(含路径重写 `/api/v1/acc/me`→`/acc/me`、身份头透传 `X-User-Id=1001|Role=CONSUMER|Mfa=true|Jti=jti-1001`、伪造头被剥离、限流 3×200 → 2×429+2004、慢下游 503+5002);故障注入:双实例停 A 后 B 继续服务。
- 【编码】任务组 10 ACC 瘦身(网关唯一鉴权点):删除 ACC `AuthFilter`/`RevocationStore`/`NoopRevocationStore` 及装配,新增 `TrustedHeaderAuthFilter`(`X-User-*` 四头 → `AuthContext`,缺/空白身份头 401 fail-closed),迁移 4 个测试文件鉴权断言(新增 `TestIdentity` 工具)。验证:ACC 251 用例 0 失败。
- 【测试】任务 10.2 真机双进程联调(嵌入式真实库):新增演练桩 `services/acc/deploy/drill/`(嵌入式 MariaDB(mariaDB4j)+ Flyway V1/V2 + `@Primary` 真实 DataSource 覆盖 M1 占位数据源 + 真实 `AccApplication` 进程,夹具账户 `account_id=1001`);链路 `curl → 网关(鉴权/限流/熔断/改写) → ACC 真实进程 → MyBatis → MariaDB` 实测。验证:15 分钟 token 200 / 1 小时 token 401+2001 / 无 `sub` token 401+2001 / 伪造 `X-User-Id:9999` 仍为 `acc_1001` / 白名单 200 / 内部接口 404(直连 ACC 同路径 200,证明拦截在网关)/ 路径混淆 7 变体全 404;ACC 直连无身份头 401、带身份头 200(`138****8000`/`张*丰`),库内 `mobile` 为 AES-GCM 密文。
- 【编码】真机联调发现并修复 ACC 数据层缺陷:Mapper Bean 原为长驻、`autoCommit=false`、永不提交的会话,真实库下「读陈旧 / 写不落库 / 持锁阻塞外部写入(约 50s)」→ 改 `factory.openSession(true)` 自动提交;新增真实库装配层回归测试 `RealDbAssemblyTest`(2 用例,反向验证 1 Failure + 1 Error);`services/acc/pom.xml` 钉 `jakarta.annotation-api:2.1.1`。验证:ACC 253 用例 0 失败;修复后外部 `SELECT` 得到 `closed_at=2026-09-15 19:52:29.212, close_reason=drill-fixed`。
- 【编码】令牌策略定档 + I9 加固(网关侧):双 token——短 token(access)**15 分钟**走 `Authorization: Bearer` 由网关校验,长 token(refresh)**7 天**存 HttpOnly + Secure + SameSite Cookie、不经网关业务链路、仅用于换发;新增 `gateway.auth.access-token-max-ttl`(默认 `15m`)/`gateway.auth.clock-skew`(默认 `60s`);`JwtVerifier` 增 `sub` 必填、`exp` 必填且不超上限、`alg` 必须 HS256。验证:网关测试 **139 用例 0 失败**(原 132)。
- 【支撑】文档口径同步:PDD **v1.16**、高并发 **v0.7**(含 ADR-8 改写)、边界基准 **v1.7**(C14 变更记录)、DEVELOPMENT_CONSTRAINTS §1、根 README、部署图 01/02/03、GATEWAY README **v1.1**、网关部署手册(切换 6 步 + 验证清单 + 回滚)、ACC README §6(已知限制与实现要点)、AI_DEV_LOG 2026-09-15(会话 5-7)、openspec 变更工件(proposal/design/spec/tasks)。

## 决策与发现

- 里程碑决策:网关由「M1 后期独立部署 + 内嵌 Filter 过渡」改为 **M1 初期交付即接管**(网关为平台唯一鉴权点,ACC 内嵌鉴权 Filter 移除,服务内仅保留业务越权校验 2002 与内部回调 Token 鉴权);兜底口径改「**双实例 + 故障快速失败 503**」,作废「网关故障 → Nginx 直连单体降级」(无鉴权直连即安全破口)。
- 令牌时效定档:短 token **15 分钟** + 长 token **7 天**(HttpOnly + Secure + SameSite Cookie,仅换发);PDD 中「JWT 时效」原为【待评审】阈值项,本次落地;**换发(refresh)接口尚未实现**,属接口清单新增,登记不臆造。
- ACC 缺陷处置口径:取「**最小修复(自动提交)+ 登记残余限制**」——长驻会话、跨表写入无原子性、并发串行化风险待 M2 收敛为「按请求会话 + 显式事务边界」,不在交付期重构数据层。
- 装配陷阱(实测):`@ConfigurationProperties` 记录绑定**只允许唯一构造器**(给嵌套记录加第二构造器 → `gateway.auth` 绑定为 `null`、12 个上下文用例同时变红),需要默认值用**静态工厂** `Auth.of(...)`;**test 作用域依赖也会经依赖调解污染运行期 API**(mariaDB4j 传递 javax 时代 `jakarta.annotation-api:1.3.5` → 容器启动 `NoClassDefFoundError`),须显式钉版。

## 问题与阻塞

- 环境受限未复演:11.1 `docker compose up`(沙箱无 Docker;已用「网关双实例 + 下游桩 / 真实 ACC」等价验证)、12.2「全实例不可用 → Nginx 503」(属 LB/Nginx 层行为,需 Nginx/K8s 环境);沙箱外部 MySQL 服务无法启动(需管理员权限),真机联调改用嵌入式 MariaDB 桩。
- 待接口清单流程(不臆造接口):refresh 换发接口与 Cookie 属性 / CSRF 口径;吊销名单写入方(ACC 登出 / 踢下线,`SET revoked:jti:{jti} 1 PX <剩余有效期>` 契约已固化、实现待登录/登出功能)。
- 提交待执行:9.2 `commit-check` 门禁;工作区另有**非本次变更的既有改动**(`.githooks/*`、`AGENT_MEMORY.md`、`docs/agent-sdlc-standard/*`、`PROJECT_LOG/*` 等),提交粒度需拆分。
- Gate C 余项:`rate-limit.lua` 独立测试。

## 明日计划

- 执行 9.2 `commit-check` 门禁并按粒度拆分提交(网关变更 / 既有改动分开);
- 有 Docker / 数据库环境时复演 11.1 与真机双进程联调(桩已就绪:`services/acc/deploy/drill/README.md`);
- 推进接口清单流程:refresh 换发接口 + 吊销名单写入方;补 `rate-limit.lua` 独立测试。
