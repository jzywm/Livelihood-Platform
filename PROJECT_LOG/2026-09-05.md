# 2026-09-05 · 每日记录

> 本日两项活动：①【支撑】DSH 工具链增强（dsh-code-intel 插件选型与落地，见 `AI_DEV_LOG/2026-09-05.md` 会话 1）；②【需求/设计】用户画像文档 `31` 完善（P10 学生画像 + 角色功能清单模板，见会话 2）。

## 今日目标

- 【支撑】解决「项目规模太大、每次新会话 Agent 重复读大文件」的痛点：找一款插件（索引 / 缓存 / 按需加载 / 增量读取）并落地到 DeepSeek Harness
- 【需求/设计】用户画像文档 `31` 迭代：§5 判断回填 + P10 学生画像 + 「角色名：功能清单」模板与完整清单

## 完成

- 【支撑】插件选型：对比 `dsh-code-intel`（符号索引 + 混合检索）/ `dsh-context-milvus`（向量索引需 Milvus）/ `dsh-token-optimizer`（增量 diff + 缓存）/ `dsh-code-index`（代码库索引），首选 **dsh-code-intel** → 验证：web_search 交叉核对 GitHub 仓库与描述
- 【支撑】dsh-code-intel 用法落地：确认**已装于 web profile**（`dsh-code-intel@0.1.3` 在 `dsh.profile.bundles`），工具 = `code_search` / `code_outline`，索引 SQLite 存 `.dsh/code-index/` 增量更新，默认词法模式 → 验证：npm registry 镜像 packument + 本地 `dist/index.js` 源码核对
- 【支撑】实测生效：`code_outline` 读 `.downloads/dsh-market-src/dsh-market-main/src/registry.ts` 返回符号清单（interface/function + 行号）→ 验证：默认配置下工具已可调用、单文件不依赖全量索引
- 【需求/设计】用户画像 `31` 升 v1.1：新增 P10「学生」画像卡（消费端第四类，痛点含培训贷/兼职暑期工/校园三小）+ 消费端痛点扩展 → 验证：证据逐条对照 `01/03/16/12问卷`，grep 落位
- 【需求/设计】监管端画像落地 §5 #6：一线执法员（单店证据包/现场查/录入轻/预警到人）vs 科室领导（态势看板/办结考核/分级监管）双视角 + 数据双轨 → 验证：P9 卡追加两段、§5 #6 回填 ✅
- 【需求/设计】新增 `31` §2.6 角色功能清单：「角色名：功能清单」模板 + 12 角色完整清单（功能点编号溯源 `32-功能列表` 79 点；门店管理员/店员作示例，标「如无特别说明/默认」）→ 验证：read §2.6 格式核对，编号与 `32` 一致

## 决策与发现

- 决策：首选 `dsh-code-intel`（无需额外中间件、索引跨会话复用）而非 `dsh-context-milvus`（需自建 Milvus）；`dsh-token-optimizer` 定位为增量 diff 补充
- 发现：插件「已装未用」——DSH 插件生效取决于 profile `package.json` 的 `dsh.profile.bundles`，而非仅仅 node_modules 存在；配置写在 `<DSH_HOME>\profiles\web\cordis.patch.yml`（个人 patch 层）
- 发现：当前沙箱外网 `find_dsh_plugin` / GitHub raw 直连不可达，npm 走 `registry.npmmirror.com` 镜像可达（`--cache` 指工作区解决 EPERM）

## 问题与阻塞

- 无新增平台阻塞（M0 运营主体未定沿用）；插件「避免重读大文件」的首个索引仍需全量读一遍，收益在首次之后（增量复用）
- 备注：PROJECT_LOG 每日文件缺 09-04 档（本会话不掌握 09-04 活动）

## 明日计划（建议）

- 【支撑】可选：给 dsh-code-intel 写显式配置（`cordis.patch.yml` 追加 code-intel config），或把「查代码优先 code_search/code_outline」固化为常驻规则
- 【需求/设计】可选：把门店级功能（商品录入/订单管理/销售订单/库存/AI经营报告/账号权限）正式落到 `32-功能列表`/PRD 并给编号；`31` §5 其余 #2~#5/#7/#8 用户已拍板、正文未回填（方案 2 可选）
- 平台主线不变（见 README「下一步」）：概要设计 v0.6 评审升 v1.0（同步需求 v1.8 非功能口径）→ 数据库设计 → PC Web 原型
