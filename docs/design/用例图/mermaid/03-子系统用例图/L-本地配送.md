# L 本地配送 · 子系统用例图（Mermaid）

> 与 `../03-子系统用例图/L-本地配送.puml` 同源；规约见 `L-本地配送-规约.md`。

```mermaid
flowchart LR
    subgraph Actors["参与者"]
        C["消费者"]
        M["商户"]
        R["骑手/配送员"]
        O["平台运营方"]
    end
    subgraph L["L 本地配送"]
        L01(["UC-SUB-L-01 选点下单并校验配送范围"])
        L02(["UC-SUB-L-02 跟踪订单状态与全程进度"])
        L03(["UC-SUB-L-03 接单备货并上报出餐"])
        L04(["UC-SUB-L-04 调度派单并处置异常"])
        L05(["UC-SUB-L-05 核验建档并管理骑手运力"])
        L06(["UC-SUB-L-06 存证位置轨迹并预警异常"])
        L07(["UC-SUB-L-07 兼容车型并承运大件货物"])
        L08(["UC-SUB-L-08 导航配送并共享实时位置"])
    end
    C --> L01
    C --> L02
    M --> L03
    O --> L04
    O --> L05
    R --> L05
    R --> L06
    R --> L07
    R --> L08
    C --> L08
    O --> L06
    O --> L07
    L02 -. include .-> L08
    L07 -. extend .-> L01
    L07 -. include .-> L04
```

> 说明：L-07 车型兼容——E_BIKE 电动车（常规小件）/ CAR 汽车（大件货物），派单/接单按车型匹配；L-08 骑手端双导航（导航到商铺/导航到配送地点，按车型唤起地图开放平台）+ 位置心跳 → 买家配送中可见、送达即切断（R-03 授权可见级）。
